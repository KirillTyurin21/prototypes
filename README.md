# iiko Прототипы

Рабочая область для создания интерактивных прототипов плагинов **iikoFront** и панелей **iikoWeb**.

## Технологический стек

- **Angular 16** (standalone-компоненты)
- **Tailwind CSS** (кастомная тема iiko)
- **Lucide Angular** (иконки)
- **Angular Router** (lazy loading)

## Быстрый старт

```bash
npm install
npm run dev          # → http://localhost:3000
```

## Структура проекта

```
materials/                   # 📁 Исходные материалы для прототипов
├── Прототип_1/              # ТЗ, скриншоты, макеты для прототипа 1
├── Прототип_2/              # ТЗ, скриншоты, макеты для прототипа 2
└── README.md                # Инструкция по работе с материалами

src/app/
├── app.component.ts         # Корневой компонент
├── app.config.ts            # Конфигурация приложения
├── app.routes.ts            # Главные маршруты
├── shared/
│   ├── icons.module.ts      # Регистрация Lucide-иконок
│   └── prototypes.registry.ts  # Реестр всех прототипов
├── components/
│   ├── layout/              # MainLayout, Sidebar, TopBar
│   └── ui/                  # UI-компоненты (Button, Card, Input, Modal, Table, Tabs, Feedback)
├── pages/
│   └── home.component.ts    # Главная страница
└── prototypes/
    └── demo/                # Демо-прототип (пример)
        ├── demo.routes.ts
        ├── demo-prototype.component.ts
        └── screens/
```

## Добавление нового прототипа

1. Создай папку `src/app/prototypes/<slug>/`
2. Создай `<slug>.routes.ts` и корневой компонент
3. Добавь `loadChildren` в `app.routes.ts`
4. Добавь запись в `prototypes.registry.ts`
5. При необходимости зарегистрируй новые иконки в `icons.module.ts`

## Команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Dev-сервер (порт 3000) |
| `npm run build` | Продакшен-сборка |
| `npm run watch` | Watch-режим |

> **Windows PowerShell**: перед npm-командами может потребоваться `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`
