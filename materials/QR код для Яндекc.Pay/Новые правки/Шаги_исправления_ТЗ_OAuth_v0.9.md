# Пошаговый план исправления ТЗ OAuth-онбординг (v0.8.2 -> v0.9)

> **Дата создания:** 10.04.2026
> **Источник:** Анализ_и_план_обновления_ТЗ_OAuth.md (14 COR корректировок)
> **ТЗ файл:** `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`
> **Лимит на шаг:** 190k токенов (один запрос Copilot)

---

## Шаг 1: Секция 9.4 - Форма организации (COR-01, COR-13)

**Волна:** 1
**Блокеры:** Нет
**Оценка токенов:** ~60k из 190k

**Цель:** Сделать обязательным только ИНН (вместо 8 полей), добавить примечания о DaData и синхронности создания.

**Контекст для чтения:**
- Файл `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`, строки 927-955 (секция 9.4, таблица полей)
- Файл `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md`, секция 1 (COR-01, COR-13)

**Корректировки:**

| COR | Действие | Секция | Строки | Тип правки |
|-----|----------|--------|--------|------------|
| COR-01 | Из 15 полей сделать обязательным ТОЛЬКО ИНН (#2). Остальные 14 - опциональны. Добавить примечание про DaData | 9.4 | ~930-948 | replace (15 строк таблицы) |
| COR-13 | Добавить примечание: "Создание организации - синхронная операция, поллинг не требуется" | 9.4 | ~954 (после блока "Поведение") | insert |

**Детальные инструкции:**

1. Прочитать строки 920-960 файла ТЗ (секция 9.4 "Форма создания организации", таблица полей и поведение)

2. Прочитать секцию 1 анализа - строки COR-01 и COR-13 в таблице корректировок

3. **COR-01 - Правка таблицы полей (строки ~930-948):**
   Заменить ВСЮ таблицу `| # | Поле | Тип | Обязательное | ...` полностью. Изменения в столбце "Обязательное":
   - Поле #1 "Название организации": `Да` -> `Нет`
   - Поле #2 "ИНН": остается `Да` (единственное обязательное)
   - Поле #3 "ОГРН": `Да` -> `Нет`
   - Поле #5 "Юридический адрес": `Да` -> `Нет`
   - Поле #11 "Контакт: Email": `Да` -> `Нет`
   - Поле #12 "Контакт: Телефон": `Да` -> `Нет`
   - Поле #13 "Контакт: Фамилия": `Да` -> `Нет`
   - Поле #14 "Контакт: Имя": `Да` -> `Нет`
   Итого: 7 полей меняют `Да` -> `Нет`, 6 полей остаются `Нет`, 1 поле ("ИНН") остается `Да`.

4. **COR-01 - Добавить примечание о DaData после таблицы полей (строка ~949):**
   Вставить после таблицы, перед секцией "Кнопки":
   ```
   [!] Важно: Обязательным является только ИНН (поле #2). Яндекс автоматически подтягивает ОГРН, КПП, наименование и адрес организации по ИНН из сервиса DaData. Остальные поля опциональны и используются для ручного заполнения в случаях, когда автоматическое определение невозможно.
   ```

5. **COR-13 - Добавить примечание о синхронности (строка ~955, после последнего абзаца "Поведение"):**
   Вставить после абзаца "Если ИНН совпадает с существующей организацией..." в секции "Поведение":
   ```
   [!] Важно: Создание организации (POST /partners) - синхронная операция. Ответ с результатом приходит сразу, поллинг в отличие от регистрации мерчанта не требуется.
   ```

6. Верифицировать: прочитать строки 920-960 результата, убедиться что в таблице только ИНН имеет `Да` в столбце "Обязательное"

**Промпт для Copilot (копировать):**

> Открой файл `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`.
> Прочитай секцию 9.4 "Форма создания организации" (строки 920-960).
> Прочитай анализ из `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md`, строки таблицы COR-01 и COR-13.
>
> Внеси 3 правки по COR-01 и COR-13:
>
> 1. В таблице полей формы (15 строк): сделай обязательным ТОЛЬКО поле #2 "ИНН" (столбец "Обязательное" = "Да"). Для всех остальных 14 полей установи "Нет". Конкретно: поля #1, #3, #5, #11, #12, #13, #14 меняются с "Да" на "Нет".
>
> 2. После таблицы полей, ПЕРЕД секцией "Кнопки", добавь примечание:
> `[!] Важно: Обязательным является только ИНН (поле #2). Яндекс автоматически подтягивает ОГРН, КПП, наименование и адрес организации по ИНН из сервиса DaData. Остальные поля опциональны и используются для ручного заполнения в случаях, когда автоматическое определение невозможно.`
>
> 3. В секции "Поведение" (после абзаца про идемпотентность ИНН) добавь примечание:
> `[!] Важно: Создание организации (POST /partners) - синхронная операция. Ответ с результатом приходит сразу, поллинг в отличие от регистрации мерчанта не требуется.`
>
> После правок прочитай результат секции 9.4 и убедись, что только ИНН помечен как обязательный.

**Чек-лист верификации:**
- [ ] В таблице полей секции 9.4 только поле #2 "ИНН" имеет "Да" в столбце "Обязательное"
- [ ] Остальные 14 полей имеют "Нет" в столбце "Обязательное"
- [ ] Примечание о DaData присутствует между таблицей полей и секцией "Кнопки"
- [ ] Примечание о синхронности операции присутствует в секции "Поведение"
- [ ] Структура таблицы не сломана (6 столбцов сохранены)

---

## Шаг 2: Секция 9.5 - Форма мерчанта (COR-02, COR-03, COR-10)

**Волна:** 1
**Блокеры:** Нет
**Оценка токенов:** ~55k из 190k

**Цель:** Все поля обязательные, заменить URL на физический адрес, удалить batch-режим.

**Контекст для чтения:**
- Файл `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`, строки 957-1032 (секция 9.5)
- Файл `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md`, секция 1 (COR-02, COR-03, COR-10)

**Корректировки:**

| COR | Действие | Секция | Строки | Тип правки |
|-----|----------|--------|--------|------------|
| COR-10 | Поле #2: "URL (сайт ресторана)" -> "Физический адрес", убрать URL-валидацию, сделать обязательным | 9.5 | ~975 | replace (1 строка таблицы) |
| COR-02 | Поле #11 "Контакт: Отчество": "Нет" -> "Да". Все поля обязательные | 9.5 | ~985 | replace (1 строка таблицы) |
| COR-03 | Удалить абзац о batch-режиме ("до 20 мерчантов за один запрос") | 9.5 | ~1013-1014 | delete (2 строки) |

**Детальные инструкции:**

1. Прочитать строки 957-1032 файла ТЗ (секция 9.5 "Форма подачи заявки на мерчанта")

2. Прочитать секцию 1 анализа - строки COR-02, COR-03, COR-10 в таблице корректировок

3. **COR-10 - Замена поля URL на физический адрес (строка ~975):**
   Заменить строку таблицы:
   ```
   | 2 | URL (сайт ресторана) | text input | Нет | Валидный URL | `merchants[].merchant.url` |
   ```
   на:
   ```
   | 2 | Физический адрес | text input | Да | Не пустое | `merchants[].merchant.address` |
   ```

4. **COR-02 - Сделать Отчество обязательным (строка ~985):**
   Заменить строку таблицы:
   ```
   | 11 | Контакт: Отчество | text input | Нет | - | `merchants[].communication_contact.middle_name` |
   ```
   на:
   ```
   | 11 | Контакт: Отчество | text input | Да | Не пустое | `merchants[].communication_contact.middle_name` |
   ```

5. **COR-03 - Удалить batch-режим. Заменить строку в секции "Поведение" (строки ~1013-1014):**
   Удалить (или заменить) строку:
   ```
   - Допускается подача заявки на несколько мерчантов за один запрос (до 20 мерчантов в одном `POST /merchants/register`).
   ```
   Заменить на:
   ```
   - Одна заявка соответствует одному ресторану (одному мерчанту). Batch-режим не поддерживается в MVP.
   ```

6. **COR-03 - Удалить абзац про batch-заявку (строки ~1018-1019):**
   Удалить строку:
   ```
   - В случае batch-заявки (несколько мерчантов) отображается список выбранных ресторанов с общим набором полей (банковские реквизиты, контактные данные).
   ```

7. Верифицировать: прочитать результат секции 9.5, убедиться что все 11 полей обязательные, URL заменен на адрес, batch-режим удален

**Промпт для Copilot (копировать):**

> Открой файл `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`.
> Прочитай секцию 9.5 "Форма подачи заявки на мерчанта" (строки 957-1032).
> Прочитай анализ из `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md` (COR-02, COR-03, COR-10).
>
> Внеси 4 правки:
>
> 1. **COR-10:** В таблице полей поле #2: замени "URL (сайт ресторана)" на "Физический адрес". Столбец "Обязательное" = "Да". Столбец "Валидация" = "Не пустое" (убрать "Валидный URL"). Столбец API-поле = `merchants[].merchant.address`.
>
> 2. **COR-02:** В таблице полей поле #11 "Контакт: Отчество": измени "Нет" на "Да", валидацию "-" на "Не пустое". Итого все 11 полей должны быть "Да".
>
> 3. **COR-03:** В секции "Поведение" замени строку "Допускается подача заявки на несколько мерчантов за один запрос (до 20 мерчантов...)" на: "Одна заявка соответствует одному ресторану (одному мерчанту). Batch-режим не поддерживается в MVP."
>
> 4. **COR-03:** Удали строку "В случае batch-заявки (несколько мерчантов) отображается список выбранных ресторанов с общим набором полей (банковские реквизиты, контактные данные)."
>
> После правок прочитай результат секции 9.5 и убедись, что все 11 полей обязательные и нет упоминаний batch-режима.

**Чек-лист верификации:**
- [ ] Поле #2 называется "Физический адрес" (не "URL")
- [ ] Поле #2 имеет "Да" в столбце "Обязательное"
- [ ] Поле #2 имеет валидацию "Не пустое" (не "Валидный URL")
- [ ] Поле #11 имеет "Да" в столбце "Обязательное"
- [ ] Все 11 полей имеют "Да" в столбце "Обязательное"
- [ ] Нет упоминания "до 20 мерчантов" или "batch-заявка"
- [ ] Есть строка "Одна заявка соответствует одному ресторану"
- [ ] Структура таблицы не сломана (6 столбцов)

---

## Шаг 3: Секция 9.6 - Экран статуса (COR-06, COR-08, COR-09, COR-11)

**Волна:** 1
**Блокеры:** Нет
**Оценка токенов:** ~65k из 190k

**Цель:** Удалить агрессивный поллинг (Стратегия 1), добавить retry-логику, переименовать кнопку, исключить UI управления токенами.

**Контекст для чтения:**
- Файл `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`, строки 1033-1097 (секция 9.6)
- Файл `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md`, секция 1 (COR-06, COR-08, COR-09, COR-11)

**Корректировки:**

| COR | Действие | Секция | Строки | Тип правки |
|-----|----------|--------|--------|------------|
| COR-08 | УДАЛИТЬ Стратегию 1 (агрессивный поллинг: 3 сек/10 сек/10 мин). Оставить только Стратегию 2 (раз в 10 мин) | 9.6 | ~1047-1065 | delete (блок Стратегии 1 ~18 строк) |
| COR-09 | Кнопка "Настроить QR-таблички" -> "Перейти в настройки", навигация в настройки ресторана | 9.6 | ~1039, ~1085 | replace (2 места) |
| COR-06 | Добавить явное исключение UI управления токенами. Добавить индикатор статуса токена | 9.6 | ~1090 (после Поведение) | insert |
| COR-11 | Добавить retry-логику: до 3 POST при сбое получения User Token | 9.6 | ~1082 (после абзаца про автозапрос токена) | insert |

**Детальные инструкции:**

1. Прочитать строки 1033-1097 файла ТЗ (секция 9.6)

2. Прочитать секцию 1 анализа - строки COR-06, COR-08, COR-09, COR-11

3. **COR-08 - Удалить Стратегию 1 (строки ~1047-1065):**
   Удалить ВЕСЬ блок от текста `**Стратегия 1 - Начальный поллинг**` до конца его примечания `[!] Важно: Стратегия 1 использует более частый опрос...` включительно.
   Это включает:
   - Заголовок "Стратегия 1 - Начальный поллинг"
   - Таблицу параметров (4 строки: Первые 30 секунд / После 30 секунд / Максимальная длительность / Назначение)
   - Примечание [!] про расхождение с Vendor Cabinet API
   
   После удаления переименовать заголовок `**Стратегия 2 - Фоновый поллинг**` в `**Стратегия поллинга статуса:**` (убрать нумерацию, т.к. стратегия теперь одна).
   
   В таблице Стратегии 2 добавить строку:
   ```
   | При открытии страницы | Однократный запрос статуса |
   ```

4. **COR-09 - Переименовать кнопку (строка ~1039 в таблице статусов и строка ~1085 в Поведении):**
   
   В таблице "Визуальное отображение статусов":
   Заменить:
   ```
   | `active` | "Подключен" | Мерчант одобрен, готов к работе | Кнопка "Настроить QR-таблички" |
   ```
   на:
   ```
   | `active` | "Подключен" | Мерчант одобрен, готов к работе | Кнопка "Перейти в настройки" |
   ```
   
   В секции "Поведение":
   Заменить:
   ```
   - При нажатии "Настроить QR-таблички" (статус `active`) - скролл к секции маппинга QR-табличек (подсекция 9.7).
   ```
   на:
   ```
   - При нажатии "Перейти в настройки" (статус `active`) - навигация в настройки ресторана (страница управления Яндекс.Пэй).
   ```

5. **COR-11 - Добавить retry-логику (после абзаца про автозапрос токена в "Поведении"):**
   После строки `...автоматически запрашивается User Token через POST /merchants/{id}/usertoken, полученный токен сохраняется в iiko.` добавить:
   ```
   - Retry-логика получения User Token: при сбое POST-запроса за токеном выполняется до 3 повторных попыток с интервалом 30 секунд. Дублирование токенов некритично - до 3 токенов могут сосуществовать для одного мерчанта.
   ```

6. **COR-06 - Добавить блок про индикатор токена (после секции "Поведение"):**
   Вставить перед секцией 9.7:
   ```
   **Индикатор статуса токена:**

   | Состояние | Текст в UI | Иконка/цвет |
   |-----------|-----------|-------------|
   | Токен получен | "Подключено" | Зеленый индикатор |
   | Токен не получен | "Ожидание токена" | Желтый индикатор |
   | Ошибка получения | "Ошибка подключения" | Красный индикатор |

   [!] Важно: Отдельный UI для управления токенами (создание, просмотр, удаление) в MVP не реализуется. User Token получается и обновляется автоматически. Пользователь видит только индикатор состояния.
   ```

7. Верифицировать: прочитать результат секции 9.6, убедиться что Стратегия 1 удалена, кнопка переименована, retry добавлен, индикатор добавлен

**Промпт для Copilot (копировать):**

> Открой файл `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`.
> Прочитай секцию 9.6 "Экран статуса заявок" (строки 1033-1097).
> Прочитай анализ из `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md` (COR-06, COR-08, COR-09, COR-11).
>
> Внеси 4 группы правок:
>
> 1. **COR-08:** УДАЛИ весь блок "Стратегия 1 - Начальный поллинг" (от заголовка до примечания [!] про расхождение с Vendor Cabinet API включительно). Переименуй "Стратегия 2 - Фоновый поллинг" в "Стратегия поллинга статуса:" (без нумерации). В таблицу стратегии добавь строку: `| При открытии страницы | Однократный запрос статуса |`.
>
> 2. **COR-09:** В таблице статусов замени "Кнопка "Настроить QR-таблички"" на "Кнопка "Перейти в настройки"". В секции "Поведение" замени "При нажатии "Настроить QR-таблички" (статус `active`) - скролл к секции маппинга QR-табличек (подсекция 9.7)" на "При нажатии "Перейти в настройки" (статус `active`) - навигация в настройки ресторана (страница управления Яндекс.Пэй)".
>
> 3. **COR-11:** В секции "Поведение", после абзаца про автозапрос User Token, добавь пункт: "Retry-логика получения User Token: при сбое POST-запроса за токеном выполняется до 3 повторных попыток с интервалом 30 секунд. Дублирование токенов некритично - до 3 токенов могут сосуществовать для одного мерчанта."
>
> 4. **COR-06:** После секции "Поведение" (перед секцией 9.7) добавь блок "Индикатор статуса токена" с таблицей из 3 состояний (Подключено/Ожидание/Ошибка) и примечанием: "[!] Важно: Отдельный UI для управления токенами (создание, просмотр, удаление) в MVP не реализуется. User Token получается и обновляется автоматически. Пользователь видит только индикатор состояния."
>
> После правок прочитай результат секции 9.6 и убедись что:
> - Нет упоминаний "Стратегия 1" или "каждые 3 секунды"
> - Кнопка называется "Перейти в настройки"
> - Есть retry-логика (до 3 попыток)
> - Есть индикатор статуса токена

**Чек-лист верификации:**
- [ ] Нет текста "Стратегия 1" в секции 9.6
- [ ] Нет текста "каждые 3 секунды" или "каждые 10 секунд" (агрессивный поллинг)
- [ ] Нет примечания [!] про расхождение с Vendor Cabinet API (оно относилось к Стратегии 1)
- [ ] Заголовок стратегии: "Стратегия поллинга статуса:" (без нумерации)
- [ ] Интервал поллинга: "1 раз в 10 минут"
- [ ] Есть строка "При открытии страницы" в таблице стратегии
- [ ] В таблице статусов: "Кнопка "Перейти в настройки"" (не "Настроить QR-таблички")
- [ ] В поведении: "навигация в настройки ресторана" (не "скролл к секции маппинга")
- [ ] Есть абзац про retry: "до 3 повторных попыток"
- [ ] Есть таблица "Индикатор статуса токена" с 3 строками
- [ ] Есть примечание про исключение UI управления токенами

---

## Шаг 4: Секции 9.2, 8.6, 13 (COR-14, COR-07 + ограничения)

**Волна:** 2
**Блокеры:** Нет
**Зависимости:** Шаги 1-3 (секции 9.4, 9.5, 9.6 уже обновлены)
**Оценка токенов:** ~65k из 190k

**Цель:** Уточнить авторизацию как точку входа в онбординг (flow), добавить пометку OUT OF SCOPE для маппинга существующих мерчантов, расширить список ограничений тремя новыми пунктами.

**Контекст для чтения:**
- `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`, строки 869-926 (секция 9.2 "Блок авторизации Яндекс ID")
- `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`, строки 746-778 (секция 8.6 "Перепривязка QR-табличек")
- `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`, строки 1598-1640 (секция 13 "Ограничения и допущения")
- `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md`, секция 1 (COR-07, COR-14)

**Корректировки:**

| COR | Действие | Строки ТЗ | Тип правки |
|-----|----------|-----------|------------|
| COR-14 | Секция 9.2: после описания Состояния 2 (Авторизован) добавить примечание о последовательном flow онбординга: Auth -> Org -> Заявка -> Статус. Авторизация - точка входа, а не вложенный элемент настроек ресторана | ~893 (после таблицы Состояния 2) | insert (примечание + таблица flow) |
| COR-07 | Секция 8.6: сам сценарий перепривязки QR-табличек остается рабочим для новых мерчантов (OAuth). Но маппинг СУЩЕСТВУЮЩИХ мерчантов Яндекс с ресторанами iiko - OUT OF SCOPE. Добавить примечание | ~747 (после заголовка 8.6) | insert (блок-цитата) |
| - | Секция 13.1: добавить 3 новых ограничения (#9, #10, #11) | ~1612 (после пункта 8) | insert (3 пункта) |

**Детальные инструкции:**

1. Прочитать строки 869-926 файла ТЗ (секция 9.2 "Блок авторизации Яндекс ID")

2. Прочитать строки 746-778 файла ТЗ (секция 8.6 "Перепривязка QR-табличек")

3. Прочитать строки 1598-1640 файла ТЗ (секция 13 "Ограничения и допущения")

4. **COR-14 - Добавить примечание о flow в секцию 9.2 (после таблицы "Состояние 2: Авторизован"):**

   После описания поведения Состояния 2 (строка с "При нажатии "Выйти"...") и перед секцией 9.3 вставить:

   ```markdown
   [!] Авторизация через Яндекс ID является точкой входа в процесс онбординга. Рекомендуемый пользовательский flow:

   | Шаг | Действие | Секция |
   |-----|----------|--------|
   | 1 | Авторизация через Яндекс ID | 9.2 (данная секция) |
   | 2 | Выбор или создание организации | 9.3, 9.4 |
   | 3 | Подача заявки на подключение мерчанта | 9.5 |
   | 4 | Ожидание статуса и автоматическое получение токена | 9.6 |

   Размещение кнопки авторизации должно быть первичным действием на экране, а не вложенным элементом в настройках ресторана. Без авторизации через Яндекс ID остальные шаги онбординга недоступны (отображается только ручной ввод User Token в поле "Ключ").
   ```

5. **COR-07 - Пометить секцию 8.6 (после заголовка "### 8.6. Перепривязка QR-табличек"):**

   ВАЖНО: секция 8.6 описывает перепривязку QR-табличек между терминалами (смену привязок). Этот сценарий остается рабочим для мерчантов, созданных через OAuth-онбординг. COR-07 же касается ДРУГОГО сценария - маппинга СУЩЕСТВУЮЩИХ мерчантов (когда у клиента УЖЕ ЕСТЬ мерчанты в ЛК Яндекса и нужно связать их с ресторанами iiko). Этот сценарий нигде не описан отдельной секцией.

   Вставить после заголовка 8.6:

   ```markdown
   > [!] OUT OF SCOPE (MVP): Маппинг существующих мерчантов Яндекс (зарегистрированных ранее через ЛК Яндекс.Пэй) с ресторанами iiko не входит в текущую версию. Сценарий перепривязки QR-табличек ниже применим только к мерчантам, созданным через OAuth-онбординг. Существующие клиенты продолжают использовать ручной ввод User Token через поле "Ключ". Автоматическое сопоставление запланировано для будущих доработок.
   ```

6. **Ограничения - Добавить 3 пункта в секцию 13.1 (после пункта 8 "Api-Key плагина iikoFront..."):**

   ```markdown
   9. **Одна заявка = один ресторан (MVP)** - каждая заявка на регистрацию мерчанта подается отдельно для одного ресторана. Batch-режим (подача нескольких мерчантов за один запрос) не реализуется в MVP
   10. **Управление токенами автоматическое** - User Token получается, обновляется и хранится автоматически без участия пользователя. Отдельный UI для ручного управления token_id (создание, просмотр, удаление) не реализуется
   11. **Маппинг существующих мерчантов Яндекс - не MVP** - автоматическое сопоставление мерчантов, ранее зарегистрированных через ЛК Яндекс.Пэй, с ресторанами iiko не входит в первую версию. Для таких клиентов сохраняется ручной ввод User Token
   ```

7. Верифицировать: прочитать результат секций 9.2, 8.6, 13.1 и проверить наличие всех добавленных блоков

**Промпт для Copilot (копировать):**

```
Открой файл 02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md.

Правка 1 (COR-14, секция 9.2):
Прочитай строки 869-926 (секция 9.2 "Блок авторизации Яндекс ID").
После ПОСЛЕДНЕГО абзаца секции "Поведение" (строка "При нажатии "Выйти" - backend очищает access_token...") и ПЕРЕД секцией "### 9.3. Список организаций", вставь блок:

[!] Авторизация через Яндекс ID является точкой входа в процесс онбординга. Рекомендуемый пользовательский flow:

| Шаг | Действие | Секция |
|-----|----------|--------|
| 1 | Авторизация через Яндекс ID | 9.2 (данная секция) |
| 2 | Выбор или создание организации | 9.3, 9.4 |
| 3 | Подача заявки на подключение мерчанта | 9.5 |
| 4 | Ожидание статуса и автоматическое получение токена | 9.6 |

Размещение кнопки авторизации должно быть первичным действием на экране, а не вложенным элементом в настройках ресторана. Без авторизации через Яндекс ID остальные шаги онбординга недоступны (отображается только ручной ввод User Token в поле "Ключ").

Правка 2 (COR-07, секция 8.6):
Прочитай строки 746-778 (секция 8.6 "Перепривязка QR-табличек").
Сразу после заголовка "### 8.6. Перепривязка QR-табличек" (перед строкой "Изменение привязки QR-табличек к кассовым терминалам") вставь блок-цитату:

> [!] OUT OF SCOPE (MVP): Маппинг существующих мерчантов Яндекс (зарегистрированных ранее через ЛК Яндекс.Пэй) с ресторанами iiko не входит в текущую версию. Сценарий перепривязки QR-табличек ниже применим только к мерчантам, созданным через OAuth-онбординг. Существующие клиенты продолжают использовать ручной ввод User Token через поле "Ключ". Автоматическое сопоставление запланировано для будущих доработок.

Правка 3 (ограничения, секция 13.1):
Прочитай строки 1598-1640 (секция 13 "Ограничения и допущения").
После пункта 8 ("Api-Key плагина iikoFront не редактируется через OAuth...") добавь 3 новых пункта:

9. **Одна заявка = один ресторан (MVP)** - каждая заявка на регистрацию мерчанта подается отдельно для одного ресторана. Batch-режим (подача нескольких мерчантов за один запрос) не реализуется в MVP
10. **Управление токенами автоматическое** - User Token получается, обновляется и хранится автоматически без участия пользователя. Отдельный UI для ручного управления token_id (создание, просмотр, удаление) не реализуется
11. **Маппинг существующих мерчантов Яндекс - не MVP** - автоматическое сопоставление мерчантов, ранее зарегистрированных через ЛК Яндекс.Пэй, с ресторанами iiko не входит в первую версию. Для таких клиентов сохраняется ручной ввод User Token

После правок прочитай измененные секции 9.2 (строки 869-930), 8.6 (строки 746-780) и 13.1 (строки 1598-1620) и убедись в корректности.
```

**Чек-лист верификации:**
- [ ] Секция 9.2: после таблицы Состояния 2 присутствует примечание [!] о flow онбординга
- [ ] Секция 9.2: таблица flow содержит 4 шага (Авторизация -> Организация -> Заявка -> Статус)
- [ ] Секция 9.2: есть текст "первичным действием на экране, а не вложенным элементом в настройках ресторана"
- [ ] Секция 8.6: заголовок "Перепривязка QR-табличек" сохранен (сценарий НЕ удален)
- [ ] Секция 8.6: есть блок-цитата [!] OUT OF SCOPE (MVP) сразу после заголовка
- [ ] Секция 8.6: пометка уточняет что OUT OF SCOPE касается маппинга СУЩЕСТВУЮЩИХ мерчантов, а не самой перепривязки
- [ ] Секция 8.6: текст содержит "применим только к мерчантам, созданным через OAuth-онбординг"
- [ ] Секция 13.1: пункт 9 "Одна заявка = один ресторан (MVP)"
- [ ] Секция 13.1: пункт 10 "Управление токенами автоматическое"
- [ ] Секция 13.1: пункт 11 "Маппинг существующих мерчантов Яндекс - не MVP"
- [ ] Секция 13.1: нумерация последовательна (1-11), нет пропусков
- [ ] Структура markdown не сломана (таблицы, списки, блок-цитаты)

---

## Шаг 5: Секции 17, 18 (открытые вопросы + история изменений)

**Волна:** 2
**Блокеры:** Нет
**Зависимости:** Шаги 1-4 (записи в историю должны перечислять все внесенные правки)
**Оценка токенов:** ~45k из 190k

**Цель:** Добавить заголовок секции 17, обновить статусы существующих вопросов #1 и #2, добавить 4 новых вопроса (#5-#8) из итогов встречи 20.03, добавить запись v0.9 в историю изменений.

**Контекст для чтения:**
- `02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md`, строки 2435-2481 (таблица вопросов без заголовка + секция 18 "История изменений")
- `05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md`, секция 5.2 (новые вопросы #5-#8)

**Корректировки:**

| COR | Действие | Строки ТЗ | Тип правки |
|-----|----------|-----------|------------|
| - | Вставить заголовок `## 17. Открытые вопросы` перед таблицей вопросов | ~2436 (перед строкой `\| # \| Вопрос`) | insert (1 строка + пустая строка) |
| - | Обновить статусы вопросов #1 и #2 на "Частично закрыт" | ~2441-2442 | replace (2 строки таблицы) |
| - | Добавить 4 новых вопроса #5-#8 (endpoints, контакты, DaData, YAML-схема) | ~2445 (после строки вопроса #4) | insert (4 строки таблицы) |
| - | Добавить запись v0.9 в таблицу истории изменений | ~2481 (после строки v0.8.2) | insert (1 строка таблицы) |

**Детальные инструкции:**

**Детальные инструкции:**

1. Прочитать строки 2430-2481 файла ТЗ (конец секции 16, таблица вопросов, секция 18)

2. Прочитать секцию 5.2 анализа (файл `Анализ_и_план_обновления_ТЗ_OAuth.md`) - новые вопросы #5-#8 с приоритетами и ответственными

3. **Добавить заголовок секции 17 (строка ~2436, ПЕРЕД таблицей вопросов):**
   
   Сейчас таблица вопросов идет без заголовка после PlantUML-диаграммы. Перед строкой `| # | Вопрос | Адресат | Срок | Статус |` вставить:
   ```markdown

   ## 17. Открытые вопросы

   ```

4. **Обновить статус вопроса #1 (строка ~2441):**
   Заменить:
   ```
   | 1 | Можно ли повторно подать заявку на подключение мерчанта после отклонения (registration_status = failed)? | Яндекс (Даниил Логатский) | 20.03.2026 | Открыт |
   ```
   на:
   ```
   | 1 | Можно ли повторно подать заявку на подключение мерчанта после отклонения (registration_status = failed)? | Яндекс (Даниил Логатский) | 20.03.2026 | Частично закрыт (кнопка "Подать повторно" подтверждена косвенно на встрече 20.03) |
   ```

5. **Обновить статус вопроса #2 (строка ~2442):**
   Заменить:
   ```
   | 2 | Подтверждение готовности API к 01.04.2026 | Яндекс (Даниил Логатский) | 20.03.2026 | Открыт |
   ```
   на:
   ```
   | 2 | Подтверждение готовности API к 01.04.2026 | Яндекс (Даниил Логатский) | 20.03.2026 | Частично закрыт (на встрече 20.03 демонстрировался работающий кабинет, формальное подтверждение не получено) |
   ```

   Вопросы #3 и #4 оставить без изменений (статус "Открыт").

6. **Добавить вопросы #5-#8 (после строки вопроса #4, перед разделителем `---`):**
   ```
   | 5 | Точные имена API endpoints: POST /merchants vs POST /merchants/register? POST /merchants/{id}/tokens vs /usertoken? Верифицировать по YAML-схеме | Кирилл (по YAML-схеме от Яндекса) | 10.04.2026 | Открыт (HIGH) |
   | 6 | Формат контактных данных в API: ФИО одной строкой (full_name) или раздельно (first_name, last_name)? | Кирилл | 10.04.2026 | Открыт (MEDIUM) |
   | 7 | Подтягивание данных организации по ИНН из DaData - на стороне Яндекса или iiko? | Яндекс (Даниил) | 10.04.2026 | Открыт (MEDIUM) |
   | 8 | Обновленная YAML-схема API (обещана Даниилом на встрече 20.03, передача через Telegram) | Яндекс (Даниил) | 10.04.2026 | Открыт (HIGH, блокер для финализации ТЗ) |
   ```

7. **Добавить запись v0.9 в историю (конец секции 18, после последней строки таблицы v0.8.2):**
   ```
   | 0.9 | 10.04.2026 | Кирилл | Корректировки по итогам встречи 20.03.2026: (1) секция 9.4 - обязательно только ИНН + синхронная операция, (2) секция 9.5 - все поля обязательные, физ. адрес вместо URL, удален batch, (3) секция 9.6 - удалена Стратегия 1 поллинга, retry токена, индикатор статуса, (4) секция 9.2 - последовательный flow, (5) секция 8.6 - OUT OF SCOPE, (6) секция 13 - 3 новых ограничения, (7) секция 17 - обновлены статусы + 4 новых вопроса |
   ```

8. Верифицировать: прочитать секции 17 и 18, убедиться в наличии заголовка, обновленных статусов, новых вопросов и записи v0.9

**Промпт для Copilot (копировать):**

```
Открой файл 02_Спецификации/Плагин_iikoFront/ТЗ_OAuth_онбординг_Яндекс_Пэй.md.
Прочитай строки 2430-2481 (конец документа: вопросы и история изменений).
Прочитай секцию 5.2 анализа из 05_Рабочие_материалы/По доработкам/Анализ_и_план_обновления_ТЗ_OAuth.md.

Внеси 4 группы правок:

1. Заголовок секции 17:
   Таблица вопросов сейчас идет БЕЗ заголовка ## 17 (сразу после PlantUML-диаграммы).
   Перед строкой "| # | Вопрос | Адресат | Срок | Статус |" добавь:

   ## 17. Открытые вопросы

2. Обновление статусов вопросов #1 и #2:
   - #1: статус "Открыт" -> "Частично закрыт (кнопка "Подать повторно" подтверждена косвенно на встрече 20.03)"
   - #2: статус "Открыт" -> "Частично закрыт (на встрече 20.03 демонстрировался работающий кабинет, формальное подтверждение не получено)"
   - #3 и #4 оставить БЕЗ изменений

3. Новые вопросы #5-#8 (добавить после строки вопроса #4):
   | 5 | Точные имена API endpoints: POST /merchants vs POST /merchants/register? POST /merchants/{id}/tokens vs /usertoken? Верифицировать по YAML-схеме | Кирилл (по YAML-схеме от Яндекса) | 10.04.2026 | Открыт (HIGH) |
   | 6 | Формат контактных данных в API: ФИО одной строкой (full_name) или раздельно (first_name, last_name)? | Кирилл | 10.04.2026 | Открыт (MEDIUM) |
   | 7 | Подтягивание данных организации по ИНН из DaData - на стороне Яндекса или iiko? | Яндекс (Даниил) | 10.04.2026 | Открыт (MEDIUM) |
   | 8 | Обновленная YAML-схема API (обещана Даниилом на встрече 20.03, передача через Telegram) | Яндекс (Даниил) | 10.04.2026 | Открыт (HIGH, блокер для финализации ТЗ) |

4. Запись v0.9 в историю (после последней строки v0.8.2 в секции 18):
   | 0.9 | 10.04.2026 | Кирилл | Корректировки по итогам встречи 20.03.2026: (1) секция 9.4 - обязательно только ИНН + синхронная операция, (2) секция 9.5 - все поля обязательные, физ. адрес вместо URL, удален batch, (3) секция 9.6 - удалена Стратегия 1 поллинга, retry токена, индикатор статуса, (4) секция 9.2 - последовательный flow, (5) секция 8.6 - OUT OF SCOPE, (6) секция 13 - 3 новых ограничения, (7) секция 17 - обновлены статусы + 4 новых вопроса |

После правок прочитай результат секций 17 (строки 2436-2450) и 18 (строки 2455-2490) и убедись:
- Есть заголовок "## 17. Открытые вопросы"
- Всего 8 строк в таблице вопросов
- Вопросы #1 и #2 имеют статус "Частично закрыт"
- Вопрос #8 помечен как блокер
- Запись v0.9 - последняя в таблице истории
```

**Чек-лист верификации:**
- [ ] Присутствует заголовок `## 17. Открытые вопросы` перед таблицей
- [ ] Вопрос #1: статус "Частично закрыт" с пояснением про встречу 20.03
- [ ] Вопрос #2: статус "Частично закрыт" с пояснением про демо кабинета
- [ ] Вопросы #3 и #4: статус "Открыт" (не изменены)
- [ ] Вопрос #5: endpoints (HIGH), ответственный Кирилл
- [ ] Вопрос #6: формат контактов (MEDIUM), ответственный Кирилл
- [ ] Вопрос #7: DaData (MEDIUM), ответственный Яндекс (Даниил)
- [ ] Вопрос #8: YAML-схема (HIGH, блокер), ответственный Яндекс (Даниил)
- [ ] Всего 8 строк в таблице вопросов (не 4)
- [ ] В секции 18 есть запись v0.9 с датой 10.04.2026
- [ ] Описание v0.9 содержит перечисление 7 групп изменений по секциям
- [ ] v0.9 - последняя строка таблицы (после v0.8.2)
- [ ] Формат таблиц не сломан (5 столбцов в вопросах, 4 столбца в истории)

---

## Сводка

| Шаг | Секция | COR | Волна | Оценка | Тип правок |
|-----|--------|-----|-------|--------|------------|
| 1 | 9.4 | COR-01, COR-13 | 1 | ~60k | 3 правки: таблица полей, DaData, синхронность |
| 2 | 9.5 | COR-02, COR-03, COR-10 | 1 | ~55k | 4 правки: адрес, отчество, удаление batch (x2) |
| 3 | 9.6 | COR-06, COR-08, COR-09, COR-11 | 1 | ~65k | 4 группы: удаление стратегии, кнопка, retry, индикатор |
| 4 | 9.2, 8.6, 13 | COR-14, COR-07 | 2 | ~65k | 3 правки: flow, OUT OF SCOPE, ограничения |
| 5 | 17, 18 | - | 2 | ~45k | 4 группы: заголовок, статусы, новые вопросы, v0.9 |
| **Итого** | | **11 COR из 14** | | **~290k** | **~18 правок** |

**Примечание:** 3 COR (COR-04, COR-05, COR-12) не требуют изменений в ТЗ (только прототип).
