# Промт-патч v1.7: Плагин iikoFront — управление роботами PUDU

---
**Версия**: 1.7
**Дата**: 2026-02-26
**Автор**: Кирилл Тюрин (системный аналитик)
**Статус**: [PENDING]
**Артефакт**: Д4-патч (Промт-патч для обновления прототипа iikoFront POS-плагина)
**Базовый документ**: Промт_прототипа_PUDU_плагин_iikoFront_v1.6_патч.md (v1.6, 2026-02-17)
**Источники**: стенограмма встречи 18.02 (Руслан, Кирилл); SPEC-003 v1.18 (правки П-1, П-2, П-3, П-4, П-5, П-6); документ правок `2026-02-26-правки-SPEC-003-по-встрече-18-02.md`
**Scope**: Шесть правок: П-1/П-2/П-3 (М18/М17), П-4 (robot_id не передаётся), П-5 (confirm-модалки убраны), П-6 (маркетинг убран из плагина)
---

## Назначение

Этот документ — **дельта-патч** к промту v1.6 плагина iikoFront. Он описывает **только изменения**, связанные с шестью решениями встречи 18.02.2026:

1. **П-1**: Убрать столбец «ID» из таблицы М18 (robot_status, экран П7) — ID робота не несёт информации для пользователя
2. **П-2**: Локализовать столбец «Текущая задача» / «Задача» — отображать русские названия вместо технических `task_type`
3. **П-3**: М17 (robot_select, экран П1) **депрекирован** — NE автоматически назначает робота для всех сценариев; `robot_id` не передаётся в API
4. **П-4**: `robot_id` **не передаётся** в запросе к NE API для всех сценариев — NE сам выбирает робота и ставит в очередь
5. **П-5**: М1, М2, М14 (confirm-модалки) **депрекированы** — один клик POS-кнопки → POST → toast без промежуточных экранов
6. **П-6**: Кнопка/сценарий «Маркетинг» **полностью убран** из плагина — маркетинг управляется через `after_action` в iikoWeb

> **Руслан** (18.02): *«ID робота никакой информации не несёт, им названия будет достаточно.»*
> **Руслан** (18.02): *«Для клиента это будет более понятно. Может быть, кто-то английский вообще не изучал.»*
> **Руслан** (18.02): *«Просто отправляйте задачу нам. А мы сами ставим очередь, смотрим, какой из них как долго работал. И уже ему распределяем задачу.»*
> **Руслан** (18.02): *«Просто надо сократить максимальное количество действий для официанта. Если у нас официант будет отправлять робота в течение двух-трёх кнопок, это хорошо. А если ему ещё что-то выбирать — они не будут этим функционалом пользоваться.»*
> **Руслан** (18.02): *«Она [кнопка маркетинга] даже не нужна, если мы там выставили настройку. И он автоматически поехал в маркетинг.»*

**Инструкция по применению**: сначала примени базовый промт `Промт_прототипа_PUDU_плагин_iikoFront.md` (v1.0), затем патчи v1.1, v1.2, v1.3, v1.4, v1.5, v1.6 и затем этот патч v1.7 — последовательно.

**Ключевые изменения v1.7:**
- М18 (П7): таблица сокращена с 4 до 3 столбцов (убран ID)
- М18 (П7) + М17 (П1): столбец «Задача» локализован через `TASK_HUMAN_NAMES`
- М17 (П1): статус изменён на **DEPRECATED** — не используется ни для одного сценария в MVP
- М1, М2, М14 (confirm-модалки): статус изменён на **DEPRECATED** — один клик → POST → toast (П-5)
- Fire-and-forget перенесён с кнопки в модалке на POS-кнопку контекста (П-5)
- `robot_id` убран из всех mock-payload'ов — NE автоматически выбирает робота (П-4)
- Toast dispatched: убрано имя робота (робот неизвестен на момент отправки, П-4)
- `marketing` удалён из mockScenarioSettings, TASK_HUMAN_NAMES, демо-панели (П-6)
- Обновлены сценарные цепочки: send_menu/cleanup/send_dish отправляются без выбора робота и без подтверждения
- Каталог ячеек: обновлены бейджи М17, М1, М2, М14
- Закрыт НВ-22 (П1 для остальных сценариев — не нужен)

---

## Совместимость с предыдущими патчами

| Патч           | Совместимость | Примечание                                    |
| -------------- | ------------- | --------------------------------------------- |
| v1.0 (базовый) | Требуется     | Базовые модалки, state machine                |
| v1.1 (D1–D8)   | Требуется     | Два контекста order/main                      |
| v1.2 (E1–E11)  | Требуется     | Каталог ячеек                                 |
| v1.3 (F1–F9)   | Требуется     | M14–M16 send_dish                             |
| v1.4 (H1–H14)  | Требуется     | M17, M18, fire-and-forget, `TASK_HUMAN_NAMES` |
| v1.5 (I1–I11)  | Требуется     | Двойное имя робота, `displayRobotNameDual()`  |
| v1.6 (J1–J3)   | Требуется     | Удаление `phrase_fail` из cleanup             |

---

## Перечень изменений v1.7

| #       | Изменение                                                         | Область    | Причина                                            | SPEC-003 |
| ------- | ----------------------------------------------------------------- | ---------- | -------------------------------------------------- | -------- |
| **K1**  | ОБНОВИТЬ M18: убрать столбец «ID» из таблицы                      | M18 шаблон | ID не несёт информации для пользователя            | П-1      |
| **K2**  | ОБНОВИТЬ M18 + M17: локализовать `task_type` в столбце «Задача»   | M18/M17    | Русские названия понятнее пользователям            | П-2      |
| **K3**  | ОБНОВИТЬ M17: статус → DEPRECATED                                 | M17        | NE автоматически назначает робота для всех задач   | П-3      |
| **K4**  | ОБНОВИТЬ контекст «Заказ»: убрать переход через M17               | Контекст   | Один клик → POST → toast (без выбора робота)       | П-3      |
| **K5**  | ОБНОВИТЬ каталог ячеек: бейджи М17                                | Каталог    | Отметка DEPRECATED в каталоге                      | П-3      |
| **K6**  | ОБНОВИТЬ сценарные цепочки: marketing-with-select                 | Цепочки    | Маркетинг через after_action (iikoWeb), не М17     | П-3      |
| **K7**  | ЗАКРЫТЬ НВ-22: П1 для остальных сценариев                         | Вопросы    | Решение: П1 не используется ни для чего в MVP      | П-3      |
| **K8**  | ОБНОВИТЬ M1, M2, M14: статус → DEPRECATED                         | Реестр     | Один клик → POST → toast (без подтверждения)       | П-5      |
| **K9**  | ОБНОВИТЬ fire-and-forget: перенос на POS-кнопку + убрать robot_id | Flow       | Inline-спиннер на POS-кнопке, payload без robot_id | П-4, П-5 |
| **K10** | ОБНОВИТЬ toast, каталог ячеек, сценарные цепочки                  | Toast/UI   | Toast без имени робота, M1/M2/M14 DEPRECATED       | П-4, П-5 |
| **K11** | УДАЛИТЬ marketing из mock-данных, TASK_HUMAN_NAMES, демо-панели   | Mock/Демо  | Маркетинг убран из плагина полностью               | П-6      |

---

## K1. ОБНОВИТЬ M18 (robot_status): убрать столбец «ID»

### ИЗМЕНИТЬ: Заголовки таблицы M18 (определены в v1.4 H3, обновлены в v1.5 I4)

**Было (v1.5, grid-cols-4):**
```html
<!-- Заголовки таблицы -->
<div class="grid grid-cols-4 gap-2 px-4 py-2 text-xs text-gray-400 border-b border-gray-600">
  <span>Имя робота</span>
  <span>ID</span>
  <span>Статус</span>
  <span>Текущая задача</span>
</div>
```

**Стало (v1.7 K1, grid-cols-3):**
```html
<!-- Заголовки таблицы — v1.7 K1: убрана колонка ID -->
<div class="grid grid-cols-3 gap-2 px-4 py-2 text-xs text-gray-400 border-b border-gray-600">
  <span>Имя робота</span>
  <span>Статус</span>
  <span>Текущая задача</span>
</div>
```

### ИЗМЕНИТЬ: Строки таблицы M18 (ячейки)

**Было (v1.5 I4, 4 колонки):**
```html
<div class="grid grid-cols-4 gap-2 items-center px-4 py-3 border-b border-gray-700/50">
  <!-- Колонка 1: Имя робота (двустрочное v1.5) -->
  <div class="flex flex-col">
    <span class="text-sm font-medium text-gray-100">
      {{ displayRobotNameDual(robot.ne_name, robot.alias, robot.robot_id).primary }}
    </span>
    @if (displayRobotNameDual(robot.ne_name, robot.alias, robot.robot_id).secondary) {
      <span class="text-xs text-gray-400">
        {{ displayRobotNameDual(robot.ne_name, robot.alias, robot.robot_id).secondary }}
      </span>
    }
  </div>
  <!-- Колонка 2: ID -->
  <span class="text-sm text-gray-300 font-mono" [title]="robot.robot_id">
    {{ robot.robot_id | slice:0:12 }}...
  </span>
  <!-- Колонка 3: Статус -->
  <div class="flex items-center gap-2">
    <span class="w-2 h-2 rounded-full" [ngClass]="{
      'bg-green-500': robot.status === 'free',
      'bg-orange-400': robot.status === 'busy',
      'bg-gray-500': robot.status === 'offline'
    }"></span>
    <span class="text-sm" [ngClass]="{
      'text-green-400': robot.status === 'free',
      'text-orange-300': robot.status === 'busy',
      'text-gray-500': robot.status === 'offline'
    }">{{ statusLabels[robot.status] }}</span>
  </div>
  <!-- Колонка 4: Текущая задача -->
  <span class="text-sm text-gray-300">
    {{ robot.current_task?.task_type || '—' }}
    @if (robot.current_task?.target_point) {
      <span class="text-gray-500"> → {{ formatPointName(robot.current_task.target_point) }}</span>
    }
  </span>
</div>
```

**Стало (v1.7 K1+K2, 3 колонки, task_type локализован):**
```html
<!-- v1.7 K1: grid-cols-3 (убрана колонка ID), K2: task_type локализован -->
<div class="grid grid-cols-3 gap-2 items-center px-4 py-3 border-b border-gray-700/50">
  <!-- Колонка 1: Имя робота (двустрочное v1.5) -->
  <div class="flex flex-col">
    <span class="text-sm font-medium text-gray-100">
      {{ displayRobotNameDual(robot.ne_name, robot.alias, robot.robot_id).primary }}
    </span>
    @if (displayRobotNameDual(robot.ne_name, robot.alias, robot.robot_id).secondary) {
      <span class="text-xs text-gray-400">
        {{ displayRobotNameDual(robot.ne_name, robot.alias, robot.robot_id).secondary }}
      </span>
    }
  </div>
  <!-- Колонка 2: Статус -->
  <div class="flex items-center gap-2">
    <span class="w-2 h-2 rounded-full" [ngClass]="{
      'bg-green-500': robot.status === 'free',
      'bg-orange-400': robot.status === 'busy',
      'bg-gray-500': robot.status === 'offline'
    }"></span>
    <span class="text-sm" [ngClass]="{
      'text-green-400': robot.status === 'free',
      'text-orange-300': robot.status === 'busy',
      'text-gray-500': robot.status === 'offline'
    }">{{ statusLabels[robot.status] }}</span>
  </div>
  <!-- Колонка 3: Текущая задача — v1.7 K2: локализован через TASK_HUMAN_NAMES -->
  <span class="text-sm text-gray-300">
    {{ robot.current_task ? (TASK_HUMAN_NAMES[robot.current_task.task_type] || robot.current_task.task_type) : '—' }}
    @if (robot.current_task?.target_point) {
      <span class="text-gray-500"> → {{ formatPointName(robot.current_task.target_point) }}</span>
    }
  </span>
</div>
```

### Визуальный результат M18 (ASCII-макет v1.7):

```
┌────────────────────────────────────────────────────────────────┐
│                      Статус роботов                            │
├────────────────────────────────────────────────────────────────┤
│  Имя робота          │  Статус     │  Текущая задача           │
│──────────────────────│─────────────│───────────────────────────│
│  Белла Зал 1         │ 🟢 Свободен │  —                        │
│   BellaBot-1         │             │                           │
│  BellaBot-2          │ 🟠 Занят    │  Доставка меню → С.5      │
│                      │             │                           │
│  Терраса             │ 🟠 Занят    │  Уборка посуды → С.7      │
│   KettyBot-1         │             │                           │
│  KettyBot-2          │ ⚪ Оффлайн  │  —                        │
│                      │             │                           │
├────────────────────────────────────────────────────────────────┤
│  Обновлено: 14:32:05           [🔄 Обновить]      [Закрыть]   │
└────────────────────────────────────────────────────────────────┘
```

> **Изменения v1.7 по сравнению с v1.5**: Убрана колонка «ID» (было 4 колонки → стало 3). Столбец «Текущая задача» показывает локализованные названия: `send_menu` → `Доставка меню`, `cleanup` → `Уборка посуды` и т.д. Используется маппинг `TASK_HUMAN_NAMES` (определён в v1.4 H11). При неизвестном `task_type` — fallback на raw-значение от NE.

---

## K2. ОБНОВИТЬ M17 (robot_select): локализовать столбец «Задача»

Аналогично M18, в M17 столбец «Задача» локализуется через `TASK_HUMAN_NAMES`.

### ИЗМЕНИТЬ: Строки таблицы M17 — ячейка «Задача» (v1.4 H2, обновлена в v1.5 I3)

**Было (v1.5):**
```html
<!-- Колонка 4: Задача -->
<span class="text-sm text-gray-300">
  {{ robot.current_task?.task_type || '—' }}
  @if (robot.current_task?.target_point) {
    <span class="text-gray-500"> → {{ formatPointName(robot.current_task.target_point) }}</span>
  }
</span>
```

**Стало (v1.7 K2):**
```html
<!-- Колонка 4: Задача — v1.7 K2: локализован через TASK_HUMAN_NAMES -->
<span class="text-sm text-gray-300">
  {{ robot.current_task ? (TASK_HUMAN_NAMES[robot.current_task.task_type] || robot.current_task.task_type) : '—' }}
  @if (robot.current_task?.target_point) {
    <span class="text-gray-500"> → {{ formatPointName(robot.current_task.target_point) }}</span>
  }
</span>
```

> **Примечание**: В M17 столбец «ID» **сохраняется** (4 колонки). Убирать ID из M17 не требуется — M17 и так **DEPRECATED** (K3). Если в будущем M17 будет реактивирован, решение о колонке ID будет принято отдельно.

---

## K3. ОБНОВИТЬ M17 (robot_select): статус → DEPRECATED

### ИЗМЕНИТЬ: Реестр модалок (раздел 2.5 в prototypes.instructions.md)

**Было:**
```
| M17 | `robot_select` | Выбор робота П1 (4 состояния) | LG | Active |
```

**Стало:**
```
| M17 | `robot_select` | Выбор робота П1 (4 состояния) | LG | **DEPRECATED** (v1.7 K3) — П1 зарезервирован для будущих этапов. NE автоматически назначает робота |
```

### Логика депрекации

M17 (`robot_select`) **не удаляется** из кода — он **сохраняется** для будущих этапов (Этап 2: хостес, эквайринг). Но в MVP ни один сценарий его не вызывает:

| Сценарий      | Ранее (v1.6)               | Теперь (v1.7)                                 |
| ------------- | -------------------------- | --------------------------------------------- |
| send_menu     | M1 (confirm) → POST        | M1 (confirm) → POST (без M17, robot_id=null)  |
| cleanup       | M2 (confirm) → POST        | M2 (confirm) → POST (без M17, robot_id=null)  |
| send_dish     | M14 (confirm) → POST       | M14 (confirm) → POST (без M17, robot_id=null) |
| qr_payment    | Авто (фискализация) → POST | Без изменений                                 |
| marketing     | Через M17 → выбор → POST   | **УБРАН из плагина** (after_action, iikoWeb)  |
| cleanup multi | M18 (select) → мультивыбор | Без изменений (М18, НЕ M17)                   |

> **Warning**: M17 НЕ вызывается ни для одного flow в MVP v1.7. Компонент сохранён «мёртвым кодом» для будущих этапов. В каталоге ячеек — помечен бейджем `DEPRECATED`.

> **Примечание**: Эта таблица отражает промежуточное состояние (после П-3). M1/M2/M14 депрекируются далее в **K8** (П-5) — см. таблицу K8 ниже.

---

## K4. ОБНОВИТЬ контекст «Заказ» и «Главный»

### ИЗМЕНИТЬ: Таблица контекстов (раздел 2.4 в prototypes.instructions.md)

**Было (v1.6):**
```
| Параметр  | Контекст «Заказ» (`order`)              | Контекст «Главный» (`main`)                  |
| --------- | --------------------------------------- | -------------------------------------------- |
| Кнопки    | 5: Меню, Уборка, Блюда, QR, (Маркетинг) | 3: Маркетинг, Уборка (столы), Статус роботов |
| ...       | ...                                     | ...                                          |
| Маркетинг | Toggle (дискуссия НВ-15)                | Через П1 (robot_select)                      |
```

**Стало (v1.7 K4):**
```
| Параметр  | Контекст «Заказ» (`order`)                           | Контекст «Главный» (`main`)                            |
| --------- | ---------------------------------------------------- | ------------------------------------------------------ |
| Кнопки    | 4: Меню, Уборка, Блюда, QR                           | 2: Уборка (столы), Статус роботов                      |
| Layout    | grid-cols-2 gap-3                                    | grid-cols-2 gap-3 (было grid-cols-3)                   |
| Уборка    | М2 (один стол)                                       | М12 (мультивыбор через М18 select)                     |
| Маркетинг | ~~Toggle~~ [УБРАН v1.7 — after_action через iikoWeb] | ~~Через П1~~ [УБРАН v1.7 — after_action через iikoWeb] |
```

> **Примечание**: Кнопка маркетинга убрана из обоих контекстов. Маркетинг управляется через `after_action` (per-robot настройка в Admin Panel, Б5) и через расписание/простой (Admin Panel, Б3). Плагин **не инициирует** маркетинг руками.

---

## K5. ОБНОВИТЬ каталог ячеек

### ИЗМЕНИТЬ: Ячейки M17 (определены в v1.4 H8)

**Было:**
```typescript
{ id: 'robot_select',             title: 'М17: Выбор робота (П1)',     category: 'modal', modal: 'robot_select', badge: 'NEW' },
{ id: 'robot_select_empty',       title: 'М17: Нет роботов',           category: 'modal', modal: 'robot_select', badge: 'EMPTY', params: { emptyState: 'no_robots' } },
{ id: 'robot_select_all_offline', title: 'М17: Все offline',           category: 'modal', modal: 'robot_select', badge: 'EDGE' },
{ id: 'robot_select_error',       title: 'М17: Ошибка загрузки',       category: 'modal', modal: 'robot_select', badge: 'ERROR' },
```

**Стало (v1.7 K5):**
```typescript
// v1.7 K5: M17 DEPRECATED — П1 зарезервирован для будущих этапов
{ id: 'robot_select',             title: 'М17: Выбор робота (П1) [DEPRECATED]', category: 'modal', modal: 'robot_select', badge: 'DEPRECATED' },
{ id: 'robot_select_empty',       title: 'М17: Нет роботов [DEPRECATED]',       category: 'modal', modal: 'robot_select', badge: 'DEPRECATED', params: { emptyState: 'no_robots' } },
{ id: 'robot_select_all_offline', title: 'М17: Все offline [DEPRECATED]',        category: 'modal', modal: 'robot_select', badge: 'DEPRECATED' },
{ id: 'robot_select_error',       title: 'М17: Ошибка загрузки [DEPRECATED]',    category: 'modal', modal: 'robot_select', badge: 'DEPRECATED' },
```

### ДОБАВИТЬ: Ячейка M18 с 3 колонками (обновлённый вид)

```typescript
// v1.7 K1+K2: M18 обновлена — 3 колонки (без ID), task_type локализован
{ id: 'robot_status_v17',         title: 'М18: Статус роботов (v1.7)', category: 'modal', modal: 'robot_status', badge: 'UPDATED' },
```

---

## K6. ОБНОВИТЬ сценарные цепочки

### ИЗМЕНИТЬ: цепочка `marketing-with-select` (определена в v1.4 H9)

**Было:**
```
| marketing-with-select | M17 → выбор → cruise indicator |
```

**Стало (v1.7 K6):**
```
| marketing-with-select | [DEPRECATED v1.7] — маркетинг убран из плагина. Управляется через after_action (iikoWeb) |
```

### Остальные цепочки — **БЕЗ ИЗМЕНЕНИЙ**

Цепочки `send-menu-ok`, `cleanup-ok`, `send_dish-full` и другие **уже** НЕ проходили через M17 — они используют confirm-модалки (M1/M2/M14) напрямую. Патч v1.7 подтверждает это как финальный дизайн.

---

## K7. ЗАКРЫТЬ открытый вопрос НВ-22

### ИЗМЕНИТЬ: таблица открытых вопросов (раздел 2.13 в prototypes.instructions.md)

**Было:**
```
| НВ-22 | П1 для остальных сценариев (не только маркетинг) | Низкая |
```

**Стало:**
```
| НВ-22 | ~~П1 для остальных сценариев (не только маркетинг)~~ **[ЗАКРЫТ v1.7, SPEC-003 П-3]**: П1 (M17) не используется ни для одного сценария в MVP. NE автоматически назначает робота. | — |
```

Также закрыть вопрос **НВ-23**:

**Было:**
```
| НВ-23 | Авто-выбор единственного свободного робота | Низкая |
```

**Стало:**
```
| НВ-23 | ~~Авто-выбор единственного свободного робота~~ **[ЗАКРЫТ v1.7, SPEC-003 П-3]**: NE автоматически выбирает и назначает робота. Логика «авто-выбор единственного» неактуальна — выбор всегда на стороне NE. | — |
```

---

## K8. ОБНОВИТЬ M1, M2, M14: статус → DEPRECATED (П-5)

### Причина

Диалоги подтверждения перед отправкой задачи **полностью убираются** из контекста заказа. Официант не должен дополнительно подтверждать действие — один клик → POST → toast.

> **Руслан** (18.02): *«Просто надо сократить максимальное количество действий для официанта. Если у нас официант будет отправлять робота в течение двух-трёх кнопок, это хорошо. А если ему ещё что-то выбирать, надо что-то думать — они не будут этим функционалом пользоваться.»*
> **Кирилл** (18.02): *«Официант и так прекрасно знает, в каком столе, потому что он уже находится [в контексте заказа]. Отправил. И он просто видит, что команда пошла.»*

### ИЗМЕНИТЬ: Реестр модалок (раздел 2.5 в prototypes.instructions.md)

**Было (v1.0/v1.3):**
```
| M1  | `send_menu_confirm`  | Подтверждение меню          | MD | Active |
| M2  | `cleanup_confirm`    | Подтверждение уборки        | MD | Active |
| M14 | `send_dish_confirm`  | Подтверждение доставки блюд | MD | Active |
```

**Стало (v1.7 K8):**
```
| M1  | `send_menu_confirm`  | Подтверждение меню          | MD | **DEPRECATED** (v1.7 K8) — П-5: один клик → POST → toast |
| M2  | `cleanup_confirm`    | Подтверждение уборки        | MD | **DEPRECATED** (v1.7 K8) — П-5: один клик → POST → toast. M12 (multi-select) — Active |
| M14 | `send_dish_confirm`  | Подтверждение доставки блюд | MD | **DEPRECATED** (v1.7 K8) — П-5: один клик → POST → toast |
```

### Логика депрекации

M1, M2, M14 **не удаляются** из кода — они **сохраняются** для возможного возврата. Но в MVP ни один flow их не вызывает:

| Сценарий      | Ранее (после K3)                        | Теперь (K8+K9)                               |
| ------------- | --------------------------------------- | -------------------------------------------- |
| send_menu     | M1 (confirm) → POST (без M17)           | POS-кнопка → POST → toast (без M1, без M17)  |
| cleanup       | M2 (confirm) → POST (без M17)           | POS-кнопка → POST → toast (без M2, без M17)  |
| send_dish     | M14 (confirm) → POST (без M17)          | POS-кнопка → POST → toast (без M14, без M17) |
| cleanup multi | M12 (select) → POST **[без изменений]** | M12 (select) → POST **[без изменений]**      |
| qr_payment    | M3 → M4 → M5/M6 **[без изменений]**     | **[без изменений]**                          |

> **Warning**: M2 (`cleanup_confirm`) DEPRECATED **только для контекста заказа (один стол)**. M12 (`cleanup_multi_select`) — по-прежнему Active (главный контекст, мультивыбор столов).

> **Примечание**: Таблица K3 (раздел K3, «Логика депрекации») показывает состояние после П-3. После П-5 (K8+K9) модалки M1/M2/M14 **также DEPRECATED** — таблица K3 **заменяется** таблицей выше.

---

## K9. ОБНОВИТЬ fire-and-forget: перенос на POS-кнопку + убрать robot_id (П-4, П-5)

### Новый паттерн

**Было (v1.4 H10)**: Пользователь нажимает POS-кнопку → открывается confirm-модалка (M1/M2/M14) → пользователь нажимает «Отправить» в модалке → inline-спиннер → POST (с robot_id) → toast.

**Стало (v1.7 K9)**: Пользователь нажимает POS-кнопку → **та же кнопка** `disabled` + inline-спиннер → POST (**без robot_id**, П-4) → toast.

### ИЗМЕНИТЬ: Кнопки контекста «Заказ» (определены в v1.1 D2)

**Было (v1.1, кнопка «Отправить меню»):**
```html
<button
  (click)="activeModal = 'send_menu_confirm'"
  class="flex flex-col items-center justify-center gap-1 p-4 bg-[#1a1a1a] hover:bg-[#252525] rounded-lg transition-colors h-14">
  <lucide-icon name="utensils" class="w-5 h-5 text-gray-300"></lucide-icon>
  <span class="text-xs text-gray-300">Отправить меню</span>
</button>
```

**Стало (v1.7 K9, inline fire-and-forget):**
```html
<!-- v1.7 K9: fire-and-forget на POS-кнопке (без M1, без robot_id) -->
<button
  (click)="handleSendTask('send_menu')"
  [disabled]="submitting"
  class="flex flex-col items-center justify-center gap-1 p-4 rounded-lg transition-colors h-14"
  [ngClass]="submitting ? 'bg-[#252525] opacity-50 cursor-not-allowed' : 'bg-[#1a1a1a] hover:bg-[#252525]'">
  @if (submitting && submittingTask === 'send_menu') {
    <lucide-icon name="loader-2" class="w-5 h-5 text-[#b8c959] animate-spin"></lucide-icon>
    <span class="text-xs text-[#b8c959]">Отправка...</span>
  } @else {
    <lucide-icon name="utensils" class="w-5 h-5 text-gray-300"></lucide-icon>
    <span class="text-xs text-gray-300">Отправить меню</span>
  }
</button>
```

> **Аналогично** для кнопок «Уборка» (lucide `trash-2`, `handleSendTask('cleanup')`) и «Доставка блюд» (lucide `package`, `handleSendTask('send_dish')`). Все три используют единый метод `handleSendTask()`.

### ДОБАВИТЬ: Метод `handleSendTask()` (заменяет submit-логику из v1.4 H10)

```typescript
// v1.7 K9: Fire-and-forget на POS-кнопке (без confirm-модалки, без robot_id)
submitting = false;
submittingTask: string | null = null;

async handleSendTask(taskType: string): Promise<void> {
  this.submitting = true;
  this.submittingTask = taskType;

  // v1.7 K9: payload БЕЗ robot_id (П-4: NE автоматически выбирает робота)
  const payload = {
    table_id: this.currentOrder.table_id,
    task_type: taskType
    // robot_id — НЕ передаётся (v1.7, П-4)
  };

  console.log('[PUDU] POST /api/tasks/', taskType, payload);

  // Имитация HTTP-запроса (1.5с)
  await new Promise(resolve => setTimeout(resolve, 1500));

  // Mock: успех
  const tableName = this.currentOrder.table_name;
  const humanName = TASK_HUMAN_NAMES[taskType] || taskType;

  // v1.7 K10: toast БЕЗ имени робота (робот неизвестен на момент отправки, П-4)
  this.addToast({
    type: 'dispatched',
    text: `${humanName} — отправлено. Стол ${tableName}`
  });

  this.submitting = false;
  this.submittingTask = null;
}
```

### ОБНОВИТЬ: Fire-and-Forget flow (раздел 2.6 в prototypes.instructions.md)

**Было (v1.4 H10):**
```
**Паттерн**: Кнопка «Отправить» → `disabled` + inline-спиннер «Отправка...» (1.5с) → Успех: `activeModal = null` + toast dispatched + polling. Ошибка: `activeModal = 'error'` (M11).
**Затронуты**: M1, M2, M12, M14, M16, send_dish-quick.
```

**Стало (v1.7 K9):**
```
**Паттерн**: POS-кнопка → `disabled` + inline-спиннер «Отправка...» (1.5с) → Успех: toast dispatched + polling. Ошибка: `activeModal = 'error'` (M11).
**Затронуты (POS-кнопка)**: send_menu, cleanup, send_dish (контекст «Заказ»).
**Затронуты (кнопка в модалке)**: M12 (cleanup multi), M16 (repeat). ~~M1, M2, M14~~ [DEPRECATED K8].
**НЕ затронуты**: M3 (QR промежуточный шаг), M18 (read-only).
```

---

## K10. ОБНОВИТЬ toast-тексты, сценарные цепочки, каталог ячеек (П-4, П-5)

### ИЗМЕНИТЬ: Toast-тексты Dispatched (раздел 2.7 в prototypes.instructions.md)

**Было (v1.5):**
```
**Тексты Dispatched** (v1.5): «{human_name} — {display_name} — отправлено. Стол {N}» (marketing — без стола).
```

**Стало (v1.7 K10):**
```
**Тексты Dispatched** (v1.7): «{human_name} — отправлено. Стол {N}» (v1.7: без имени робота — робот неизвестен на момент отправки, П-4). ~~marketing~~ [УДАЛЁН П-6].
```

> **Примечание**: Имя робота убрано из toast при отправке, т.к. NE назначает робота асинхронно **после** постановки задачи (П-4). Completed-toast (если показываются) по-прежнему могут содержать имя робота — оно получено через polling.

### ИЗМЕНИТЬ: human_name маппинг (раздел 2.7 в prototypes.instructions.md)

**Было (v1.5):**
```
**human_name маппинг**: send_menu → «Доставка меню», cleanup → «Уборка посуды», cleanup_auto → «Авто-уборка», qr_payment → «QR-оплата», send_dish → «Доставка блюд», marketing → «Маркетинг-круиз».
```

**Стало (v1.7 K10):**
```
**human_name маппинг**: send_menu → «Доставка меню», cleanup → «Уборка посуды», cleanup_auto → «Авто-уборка», qr_payment → «QR-оплата», send_dish → «Доставка блюд». ~~marketing → «Маркетинг-круиз»~~ [УДАЛЁН v1.7 П-6].
```

### ИЗМЕНИТЬ: Сценарные цепочки (раздел 2.9 в prototypes.instructions.md)

**Было (v1.4, после fire-and-forget):**
```
| send-menu-ok         | M1 (3s) → null + toast dispatched                         |
| cleanup-ok           | M2 (3s) → null + toast dispatched                         |
| send_dish-full       | M14 (3s) → null + toast dispatched                        |
| fire-and-forget-full | M1 → спиннер → dispatched (5s) → completed                |
```

**Стало (v1.7 K10):**
```
| send-menu-ok         | POS-кнопка (1.5s спиннер) → toast dispatched               |
| cleanup-ok           | POS-кнопка (1.5s спиннер) → toast dispatched               |
| send_dish-full       | POS-кнопка (1.5s спиннер) → toast dispatched               |
| fire-and-forget-full | POS-кнопка → спиннер → dispatched (5s) → completed         |
```

### ИЗМЕНИТЬ: Каталог ячеек M1, M2, M14 (определены в v1.0 и v1.3)

**Было:**
```typescript
{ id: 'modal-send-menu',  title: 'М1: Подтверждение меню',     category: 'modal', modal: 'send_menu_confirm', badge: 'OK' },
{ id: 'modal-cleanup',    title: 'М2: Подтверждение уборки',   category: 'modal', modal: 'cleanup_confirm', badge: 'OK' },
{ id: 'modal-send-dish',  title: 'М14: Подтверждение доставки', category: 'modal', modal: 'send_dish_confirm', badge: 'NEW' },
```

**Стало (v1.7 K10):**
```typescript
// v1.7 K10: M1, M2, M14 DEPRECATED — П-5: один клик → POST → toast
{ id: 'modal-send-menu',  title: 'М1: Подтверждение меню [DEPRECATED]',     category: 'modal', modal: 'send_menu_confirm', badge: 'DEPRECATED' },
{ id: 'modal-cleanup',    title: 'М2: Подтверждение уборки [DEPRECATED]',   category: 'modal', modal: 'cleanup_confirm', badge: 'DEPRECATED' },
{ id: 'modal-send-dish',  title: 'М14: Подтверждение доставки [DEPRECATED]', category: 'modal', modal: 'send_dish_confirm', badge: 'DEPRECATED' },
```

### ДОБАВИТЬ: Ячейки для нового fire-and-forget паттерна (POS-кнопка)

```typescript
// v1.7 K10: Fire-and-forget на POS-кнопке (замена M1/M2/M14)
{ id: 'pos-button-submitting',  title: 'POS: Кнопка в режиме отправки (спиннер)', category: 'state', badge: 'NEW' },
{ id: 'pos-button-after-send',  title: 'POS: После отправки (toast dispatched)',   category: 'state', badge: 'NEW' },
```

---

## K11. УДАЛИТЬ marketing из mock-данных, TASK_HUMAN_NAMES, демо-панели (П-6)

### Причина

Маркетинг полностью убран из плагина iikoFront (П-6). Управляется через `after_action` per-robot в Admin Panel (iikoWeb). Плагин **не инициирует** маркетинг.

> **Руслан** (18.02): *«Она [кнопка маркетинга] даже не нужна, если мы там выставили настройку. И он автоматически поехал в маркетинг.»*

### ИЗМЕНИТЬ: mockScenarioSettings (определено в v1.0, обновлено в v1.3, v1.4, v1.6)

**Было (6 сценариев):**
```typescript
const mockScenarioSettings = {
  send_menu: { /* ... */ },
  send_dish: { /* ... */ },
  cleanup: { /* ... */ },
  cleanup_auto: { /* ... */ },
  qr_payment: { /* ... */ },
  marketing: {
    phrase: 'Здравствуйте! Попробуйте наши новинки!',
    phrase_url: '',
    cruise_points: ['lobby', 'entrance'],
    mode: 'cruise'
  }
};
```

**Стало (v1.7 K11, 5 сценариев — marketing удалён):**
```typescript
// v1.7 K11: marketing удалён (П-6: управляется через iikoWeb after_action, не через плагин)
const mockScenarioSettings = {
  send_menu: { /* ... */ },
  send_dish: { /* ... */ },
  cleanup: { /* ... */ },
  cleanup_auto: { /* ... */ },
  qr_payment: { /* ... */ }
  // marketing — УДАЛЁН v1.7 П-6
};
```

### ИЗМЕНИТЬ: TASK_HUMAN_NAMES (определено в v1.4 H11)

**Было:**
```typescript
const TASK_HUMAN_NAMES: Record<string, string> = {
  send_menu: 'Доставка меню',
  send_dish: 'Доставка блюд',
  cleanup: 'Уборка посуды',
  cleanup_auto: 'Авто-уборка',
  qr_payment: 'QR-оплата',
  marketing: 'Маркетинг-круиз'
};
```

**Стало (v1.7 K11):**
```typescript
// v1.7 K11: marketing удалён (П-6)
const TASK_HUMAN_NAMES: Record<string, string> = {
  send_menu: 'Доставка меню',
  send_dish: 'Доставка блюд',
  cleanup: 'Уборка посуды',
  cleanup_auto: 'Авто-уборка',
  qr_payment: 'QR-оплата'
  // marketing — УДАЛЁН v1.7 П-6
};
```

### ИЗМЕНИТЬ: Демо-панель (раздел 2.11 в prototypes.instructions.md)

**Было (10 элементов):**
```
| 1   | Контекст: Заказ / Главный | Toggle   | Переключение PuduContextType |
| 2   | Маркетинг: ВКЛ/ВЫКЛ       | Toggle   | Переключение круиза          |
| 3   | Имитация ошибки NE        | Button   | Показ M11                    |
| 4   | Имитация E-STOP           | Button   | Error toast                  |
| 5   | Робот в пути (pickup)     | Button   | Показ M15                    |
| 6   | Блюда приняты гостем      | Button   | Закрытие M15                 |
| 7   | E-STOP (повторная)        | Button   | Toast с меткой «ПОВТОРНО»    |
| 8   | Режим уборки              | Selector | Ручной/Авто/Смешанный        |
| 9   | Уведомления завершения    | Toggle   | show_success_notifications   |
| 10  | Задача завершена          | Button   | Toast completed (зелёный)    |
```

**Стало (v1.7 K11, 9 элементов — маркетинг-toggle удалён, нумерация пересчитана):**
```
| 1   | Контекст: Заказ / Главный  | Toggle   | Переключение PuduContextType  |
| 2   | Имитация ошибки NE         | Button   | Показ M11                     |
| 3   | Имитация E-STOP            | Button   | Error toast                   |
| 4   | Робот в пути (pickup)      | Button   | Показ M15                     |
| 5   | Блюда приняты гостем       | Button   | Закрытие M15                  |
| 6   | E-STOP (повторная)         | Button   | Toast с меткой «ПОВТОРНО»     |
| 7   | Режим уборки               | Selector | Ручной/Авто/Смешанный         |
| 8   | Уведомления завершения     | Toggle   | show_success_notifications    |
| 9   | Задача завершена           | Button   | Toast completed (зелёный)     |
```

> **Примечание**: Маркетинг-toggle удалён, т.к. плагин не управляет маркетингом (П-6). Маркетинг настраивается через Admin Panel (after_action per-robot на Э1, idle_timeout_sec на Э4 вкладка Marketing).

### ИЗМЕНИТЬ: Mock-данные (раздел 2.10 в prototypes.instructions.md)

**Было:**
```
| Настройки | `mockScenarioSettings` | 6 сценариев | send_menu, cleanup, cleanup_auto, qr_payment, send_dish, marketing |
```

**Стало (v1.7 K11):**
```
| Настройки | `mockScenarioSettings` | 5 сценариев | send_menu, cleanup, cleanup_auto, qr_payment, send_dish. ~~marketing~~ [УДАЛЁН v1.7 П-6] |
```

---

## Обновление prototypes.instructions.md

При применении этого патча обновить state-файл:

### Метаданные

```
> **Плагин iikoFront**: v1.7 (базовый v1.0 + патчи v1.1, v1.2, v1.3, v1.4, v1.5, v1.6, v1.7)
```

### Реестр модалок (раздел 2.5)

```
| M1  | `send_menu_confirm`  | Подтверждение меню          | MD | **DEPRECATED** (v1.7 K8) — П-5 |
| M2  | `cleanup_confirm`    | Подтверждение уборки        | MD | **DEPRECATED** (v1.7 K8) — П-5. M12 Active |
| M14 | `send_dish_confirm`  | Подтверждение доставки блюд | MD | **DEPRECATED** (v1.7 K8) — П-5 |
| M17 | `robot_select`       | Выбор робота П1 (4 состояния) | LG | **DEPRECATED** (v1.7 K3) |
```

### Хронология патчей (раздел 2.12)

```
| **v1.7**  | 2026-02-26 | K1–K11 | П-1: М18 убран столбец ID (3 колонки). П-2: `task_type` локализован через `TASK_HUMAN_NAMES`. П-3: M17 (П1) → DEPRECATED — NE авто-назначает робота. П-4: `robot_id` не передаётся в API, убран из toast dispatched. П-5: M1/M2/M14 → DEPRECATED — один клик → POST → toast, fire-and-forget на POS-кнопке. П-6: Marketing удалён из плагина (mock, TASK_HUMAN_NAMES, демо-панель, цепочки). НВ-22, НВ-23 закрыты. |
```

### Нумерация (раздел 4.3)

```
- Плагин: текущая серия **K** (v1.7). Следующий патч: серия **L** → v1.8
```

### Два контекста (раздел 2.4)

```
| Параметр  | Контекст «Заказ» (`order`)                           | Контекст «Главный» (`main`)                      |
| --------- | ---------------------------------------------------- | ------------------------------------------------ |
| Кнопки    | 4: Меню, Уборка, Блюда, QR                           | 2: Уборка (столы), Статус роботов                |
| Layout    | grid-cols-2 gap-3                                    | grid-cols-2 gap-3 (было grid-cols-3)             |
| Уборка    | handleSendTask('cleanup') → toast                    | М12 (мультивыбор через М18 select)               |
| Маркетинг | ~~Toggle~~ [УБРАН v1.7 — after_action через iikoWeb] | ~~Через П1~~ [УБРАН v1.7 — after_action iikoWeb] |
```

### Fire-and-Forget flow (раздел 2.6)

```
**Паттерн**: POS-кнопка → `disabled` + inline-спиннер «Отправка...» (1.5с) → Успех: toast dispatched + polling. Ошибка: `activeModal = 'error'` (M11).
**Затронуты (POS-кнопка)**: send_menu, cleanup, send_dish (контекст «Заказ»).
**Затронуты (кнопка в модалке)**: M12 (cleanup multi), M16 (repeat). ~~M1, M2, M14~~ [DEPRECATED K8].
**НЕ затронуты**: M3 (QR промежуточный шаг), M18 (read-only).
**Deprecated**: M9 (loading) и M10 (success) — заменены inline-спиннером + toast (v1.4). M1, M2, M14 — заменены fire-and-forget на POS-кнопке (v1.7).
```

### Toast-тексты (раздел 2.7)

```
**Тексты Dispatched** (v1.7): «{human_name} — отправлено. Стол {N}» (без имени робота — робот неизвестен на момент отправки, П-4). ~~marketing~~ [УДАЛЁН П-6].
**human_name маппинг**: send_menu → «Доставка меню», cleanup → «Уборка посуды», cleanup_auto → «Авто-уборка», qr_payment → «QR-оплата», send_dish → «Доставка блюд». ~~marketing → «Маркетинг-круиз»~~ [УДАЛЁН v1.7 П-6].
```

### Демо-панель (раздел 2.11)

Пересчитать нумерацию: 10 → 9 элементов (элемент «Маркетинг: ВКЛ/ВЫКЛ» удалён).

### Сценарные цепочки (раздел 2.9)

```
| send-menu-ok         | POS-кнопка (1.5s спиннер) → toast dispatched               |
| cleanup-ok           | POS-кнопка (1.5s спиннер) → toast dispatched               |
| send_dish-full       | POS-кнопка (1.5s спиннер) → toast dispatched               |
| fire-and-forget-full | POS-кнопка → спиннер → dispatched (5s) → completed         |
| marketing-with-select | [DEPRECATED v1.7] — маркетинг убран из плагина (after_action iikoWeb) |
```

### Mock-данные (раздел 2.10)

```
| Настройки | `mockScenarioSettings` | 5 сценариев | send_menu, cleanup, cleanup_auto, qr_payment, send_dish. ~~marketing~~ [УДАЛЁН v1.7 П-6] |
```

---

*Конец патча v1.7*
