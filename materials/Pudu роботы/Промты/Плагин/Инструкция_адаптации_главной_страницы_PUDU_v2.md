# Инструкция для Copilot: Адаптация главной страницы прототипа PUDU Plugin

---
**Версия**: 2.0
**Дата**: 2026-02-13
**Автор**: Кирилл Тюрин (системный аналитик)
**Статус**: [PENDING]
**Артефакт**: Инструкция по рефакторингу главной страницы front-pudu-plugin
**Базовый прототип**: `src/app/prototypes/front-pudu-plugin/`
**Эталон паттерна**: `src/app/prototypes/front-plugins/` (Premium Bonus)
---

## Назначение документа

Этот документ — **инструкция для AI-ассистента (Copilot)**, описывающая как переработать главную страницу прототипа плагина PUDU для iikoFront. Цель — перейти от текущей монолитной архитектуры (один компонент, всё на одной странице) к **каталогу ячеек-состояний** по паттерну прототипа **Premium Bonus** (`/prototype/front-plugins/premium-bonus`).

> **Что такое «каталог ячеек»**: Главная страница прототипа показывает **сетку карточек**, где каждая карточка — это одно конкретное состояние или сценарий окна плагина. Нажатие на карточку открывает соответствующий POS-диалог. Это позволяет демонстратору быстро показать любое состояние без прохождения всей цепочки действий.

---

## Мультиагентная система и план задач

### ОБЯЗАТЕЛЬНО: Используй мультиагентный подход

1. **Координатор** (основной агент) — планирует, декомпозирует, контролирует
2. **Sub-агенты** — выполняют отдельные задачи:
   - Исследование текущего кода (чтение файлов, анализ компонентов)
   - Создание новых компонентов/экранов
   - Рефакторинг существующих компонентов
   - Проверка ошибок и компиляции

### ОБЯЗАТЕЛЬНО: Веди todo-лист через `manage_todo_list`

Рекомендуемая декомпозиция:

```
1. Исследовать текущую структуру front-pudu-plugin (sub-агент)
2. Исследовать эталон Premium Bonus — PluginDialogsScreenComponent (sub-агент)
3. Создать новый main-screen компонент с сеткой ячеек
4. Создать mock-данные каталога ячеек (dialogEntries)
5. Адаптировать маршруты (добавить children)
6. Перенести логику открытия диалогов в main-screen
7. Добавить группировку ячеек по секциям (контексты + сценарии)
8. Проверить что все 12+ диалогов открываются из сетки
9. Проверка компиляции и запуск
```

---

## Раздел 1. Эталонный паттерн (Premium Bonus)

### Как устроена сетка ячеек в Premium Bonus

**Файл-эталон**: `src/app/prototypes/front-plugins/screens/plugin-dialogs-screen.component.ts`

**Архитектура**:

```
Главная страница (сетка)
├── Ячейка 1: "Поиск клиента"    → клик → activeModal = 'search'    → показывает <pos-customer-search-dialog>
├── Ячейка 2: "Клиент найден"    → клик → activeModal = 'found'     → показывает <pos-customer-found-dialog>
├── Ячейка 3: "Клиент не найден" → клик → activeModal = 'not-found' → показывает <pos-customer-not-found-dialog>
├── ...
└── Ячейка N: "Регистрация"      → клик → activeModal = 'registration' → показывает <pos-registration-dialog>
```

**Ключевые паттерны**:

1. **Сетка**: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
2. **Ячейки**: итерация через `*ngFor="let dialog of dialogs"`, каждая ячейка — кликабельная карточка
3. **Управление модалками**: переменная `activeModal: ModalType = null`
4. **Все диалоги в DOM**: каждый компонент-диалог всегда отрисован, управляется через `[open]="activeModal === 'тип'"`
5. **Закрытие**: `closeDialog()` → `activeModal = null`
6. **Данные ячеек**: массив объектов `DialogEntry` с полями `id`, `label`, `description`, `icon`, `iconColor`, `modalType`

### Структура одной ячейки (карточки)

```html
<div *ngFor="let dialog of dialogs"
     (click)="openDialog(dialog.modalType)"
     class="bg-white rounded-xl border border-gray-200 p-5
            hover:shadow-md hover:border-blue-300 cursor-pointer
            transition-all duration-200 group">

  <!-- Иконка в цветном круге -->
  <div class="w-12 h-12 rounded-xl flex items-center justify-center mb-3"
       [style.background-color]="dialog.iconColor + '20'">
    <lucide-icon [name]="dialog.icon" [size]="24"
                 [style.color]="dialog.iconColor"></lucide-icon>
  </div>

  <!-- Заголовок -->
  <h3 class="font-medium text-gray-900 mb-1">{{ dialog.label }}</h3>

  <!-- Описание -->
  <p class="text-sm text-gray-500">{{ dialog.description }}</p>
</div>
```

---

## Раздел 2. Адаптация для PUDU Plugin

### 2.1. Принципиальные отличия от Premium Bonus

У PUDU-плагина есть особенности, которых нет у Premium Bonus:

| Аспект | Premium Bonus | PUDU Plugin |
|--------|--------------|-------------|
| Контексты вызова | Один (из экрана кассы) | **Два**: из заказа (`order`) и с главного экрана (`main`) |
| Типы ячеек | Только состояния диалогов | **Состояния** + **Сценарии** (цепочки переходов) |
| Демо-панель | Нет | Есть (E-STOP, NE-ошибки, переключение) |
| Уведомления (toast) | Нет | Есть (E-STOP, ошибки) |
| Фоновый экран | Нет (просто сетка) | **Имитация POS-терминала** под диалогами |

### 2.2. Структура главной страницы PUDU (желаемая)

Страница разбита на **секции** с заголовками. Каждая секция содержит ячейки одного типа:

```
┌─────────────────────────────────────────────────────────────┐
│  ← Назад    PUDU — Каталог состояний и сценариев           │
│                                                             │
│  📍 КОНТЕКСТ: ИЗ ЗАКАЗА                                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ Отправить│  │ Уборка   │  │ Доставка │                  │
│  │ меню     │  │ посуды   │  │ блюд     │                  │
│  │ (1 стол) │  │ (1 стол) │  │ [BLOCKED]│                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│                                                             │
│  📍 КОНТЕКСТ: ГЛАВНЫЙ ЭКРАН                                │
│  ┌──────────┐  ┌──────────┐                                │
│  │ Уборка   │  │ Маркетинг│                                │
│  │ (мульти- │  │ круиз    │                                │
│  │  выбор)  │  │ (toggle) │                                │
│  └──────────┘  └──────────┘                                │
│                                                             │
│  📋 СЦЕНАРИИ (цепочки переходов)                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ QR-оплата│  │ Отправка │  │ Уборка   │                  │
│  │ (полный  │  │ меню →   │  │ (мульти) │                  │
│  │  цикл)   │  │ loading →│  │ → loading│                  │
│  │          │  │ success  │  │ → success│                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│                                                             │
│  💬 СОСТОЯНИЯ ДИАЛОГОВ (отдельные модалки)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ М1       │  │ М2       │  │ М3       │                  │
│  │ Подтвер- │  │ Подтвер- │  │ QR: фаза │                  │
│  │ ждение   │  │ ждение   │  │ «Кассир» │                  │
│  │ меню     │  │ уборки   │  │          │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ М4       │  │ М5       │  │ М6       │                  │
│  │ QR: фаза │  │ QR:      │  │ QR:      │                  │
│  │ «Гость»  │  │ Успех    │  │ Тайм-аут │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ М7       │  │ М8       │  │ М9       │                  │
│  │ Стол не  │  │ Доставка │  │ Loading  │                  │
│  │ замаплен │  │ [BLOCKED]│  │          │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ М10      │  │ М11      │  │ М12      │                  │
│  │ Успех    │  │ Ошибка   │  │ Мульти-  │                  │
│  │ (задача) │  │          │  │ выбор    │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
│                                                             │
│  🔔 УВЕДОМЛЕНИЯ И СПЕЦИАЛЬНЫЕ СОСТОЯНИЯ                    │
│  ┌──────────┐  ┌──────────┐                                │
│  │ E-STOP   │  │ Toast:   │                                │
│  │ уведом-  │  │ ошибка NE│                                │
│  │ ление    │  │          │                                │
│  └──────────┘  └──────────┘                                │
└─────────────────────────────────────────────────────────────┘
```

---

## Раздел 3. Каталог ячеек (полный перечень)

### 3.1. Секция «Контекст: Из заказа»

Ячейки этой секции при клике открывают **имитацию POS-экрана заказа** с уже запущенным действием. Контекст = `order`, стол известен из заказа.

| # | ID ячейки | Название | Иконка | Цвет | Описание | При клике |
|---|-----------|----------|--------|------|----------|-----------|
| 1 | `ctx-order-send-menu` | Отправить меню (из заказа) | `utensils` | `#b8c959` (лайм) | Кассир нажимает кнопку «Отправить меню» из экрана заказа. Стол определён автоматически | Показать POS-экран заказа → модалка М1 (send_menu_confirm) |
| 2 | `ctx-order-cleanup` | Уборка посуды (из заказа) | `spray-can` | `#b8c959` | Уборка одного стола из контекста заказа | POS-экран заказа → М2 (cleanup_confirm) |
| 3 | `ctx-order-send-dish` | Доставка блюд [BLOCKED] | `package` | `#ef4444` (красный) | Кнопка видна, но функционал заблокирован — нет терминала на раздаче | POS-экран заказа → М8 (send_dish_blocked) |

### 3.2. Секция «Контекст: Главный экран»

Ячейки этой секции показывают действия, доступные с **главного экрана iikoFront** (нет контекста заказа/стола).

| # | ID ячейки | Название | Иконка | Цвет | Описание | При клике |
|---|-----------|----------|--------|------|----------|-----------|
| 4 | `ctx-main-cleanup-multi` | Уборка посуды (мультивыбор) | `spray-can` | `#b8c959` | Официант выбирает несколько столов для уборки одновременно | POS-главный экран → М12 (cleanup_multi_select) |
| 5 | `ctx-main-marketing` | Маркетинговый круиз | `megaphone` | `#3b82f6` (синий) | Toggle-кнопка запуска маркетингового патрулирования зала | POS-главный экран → показать включение/выключение маркетинга |

### 3.3. Секция «Сценарии (цепочки переходов)»

Каждая ячейка **запускает полный сценарий** — автоматическую цепочку переходов между модалками. Это позволяет демонстратору показать весь user journey одним кликом.

| # | ID ячейки | Название | Иконка | Цвет | Описание | Цепочка переходов |
|---|-----------|----------|--------|------|----------|-------------------|
| 6 | `scenario-qr-full` | QR-оплата (полный цикл) | `qr-code` | `#b8c959` | Двухфазный сценарий: кассир → робот → гость → оплата | М3 (кассир, 30с) → loading → М4 (гость, 120с) → М5 (успех, 3с) |
| 7 | `scenario-qr-timeout` | QR-оплата (тайм-аут) | `clock` | `#f97316` (оранж) | Сценарий когда гость не успел оплатить | М3 (кассир) → loading → М4 (гость) → М6 (тайм-аут) |
| 8 | `scenario-send-menu-ok` | Отправка меню → Успех | `check-circle-2` | `#22c55e` (зелёный) | Полный успешный цикл отправки меню | М1 (подтверждение) → М9 (loading, 3с) → М10 (успех, 2с) |
| 9 | `scenario-send-menu-err` | Отправка меню → Ошибка | `alert-circle` | `#ef4444` | Цикл с ошибкой (робот недоступен, NE не отвечает) | М1 (подтверждение) → М9 (loading, 3с) → М11 (ошибка) |
| 10 | `scenario-cleanup-ok` | Уборка → Успех | `check-circle-2` | `#22c55e` | Полный успешный цикл уборки | М2 (подтверждение) → М9 (loading) → М10 (успех) |
| 11 | `scenario-cleanup-multi-ok` | Уборка (мульти) → Успех | `check-circle-2` | `#22c55e` | Мультивыбор столов → успех | М12 (выбор столов) → М9 (loading) → М10 (успех) |
| 12 | `scenario-unmapped` | Стол не замаплен | `map-pin-off` | `#f97316` | Попытка действия со столом без маппинга | М7 (предупреждение + ссылка на Web) |

### 3.4. Секция «Состояния диалогов (отдельные модалки)»

Каждая ячейка открывает **ровно один** диалог — для детального просмотра UI каждого окна.

| # | ID ячейки | Модалка | Название | Иконка | Цвет | Описание |
|---|-----------|---------|----------|--------|------|----------|
| 13 | `modal-send-menu` | М1 | Подтверждение отправки меню | `utensils` | `#b8c959` | Карточка с информацией о задаче (стол, робот, фразы) + кнопки Отмена/Отправить |
| 14 | `modal-cleanup` | М2 | Подтверждение уборки | `spray-can` | `#b8c959` | Карточка: стол, робот, фраза, время ожидания |
| 15 | `modal-qr-cashier` | М3 | QR: фаза «Кассир» | `printer` | `#b8c959` | Иконка принтера, фраза, обратный таймер 30 сек, кнопка «Отправить к гостю» |
| 16 | `modal-qr-guest` | М4 | QR: фаза «Гость» | `qr-code` | `#b8c959` | Mock QR-код, таймер 120 сек, кнопка «Оплата подтверждена» |
| 17 | `modal-qr-success` | М5 | QR: Оплата прошла | `check-circle-2` | `#22c55e` | Иконка успеха, автозакрытие 3 сек |
| 18 | `modal-qr-timeout` | М6 | QR: Тайм-аут | `clock` | `#f97316` | Предупреждение, робот возвращается на базу |
| 19 | `modal-unmapped` | М7 | Стол не замаплен | `map-pin-off` | `#f97316` | Предупреждение + ссылка на iikoWeb → Маппинг |
| 20 | `modal-dish-blocked` | М8 | Доставка блюд [BLOCKED] | `package` | `#ef4444` | Заглушка: баннер «Требуется решение», пометка «В разработке» |
| 21 | `modal-loading` | М9 | Loading (универсальный) | `loader-2` | `#6b7280` (серый) | Спиннер, **светлая тема**, closable=false, автозакрытие 3 сек |
| 22 | `modal-success` | М10 | Задача создана (успех) | `check-circle-2` | `#22c55e` | Иконка успеха, автозакрытие 2 сек |
| 23 | `modal-error` | М11 | Ошибка | `alert-circle` | `#ef4444` | **Светлая тема**, иконка ошибки, кнопки «Повторить» / «Закрыть» |
| 24 | `modal-cleanup-multi` | М12 | Мультивыбор столов | `layout-grid` | `#b8c959` | Сетка столов 3 колонки, toggle-выбор, disabled для незамапленных |

### 3.5. Секция «Уведомления и специальные состояния»

Ячейки, которые демонстрируют **не модалки, а toast-уведомления и overlay-состояния**.

| # | ID ячейки | Название | Иконка | Цвет | Описание | При клике |
|---|-----------|----------|--------|------|----------|-----------|
| 25 | `notify-estop` | E-STOP (аварийная кнопка) | `octagon` | `#ef4444` | Уведомление об аварийной остановке робота. Повторяется каждые 5 сек | Показать POS-экран + toast-уведомление E-STOP (пульсирующее) |
| 26 | `notify-ne-error` | Ошибка связи NE | `wifi-off` | `#f97316` | Toast-уведомление о потере связи с API Next Era | Показать POS-экран + toast-уведомление об ошибке NE |

**Итого: 26 ячеек** (3 контекст заказа + 2 контекст главного + 7 сценариев + 12 модалок + 2 уведомления).

---

## Раздел 4. Технические требования к реализации

### 4.1. Новая структура файлов

```
src/app/prototypes/front-pudu-plugin/
├── pudu-plugin.routes.ts               ← ИЗМЕНИТЬ: добавить children
├── pudu-plugin-prototype.component.ts  ← ИЗМЕНИТЬ: только router-outlet
├── types.ts                            ← ИЗМЕНИТЬ: добавить CellEntry, CellSection
├── screens/                            ← СОЗДАТЬ папку
│   ├── pudu-catalog-screen.component.ts    ← СОЗДАТЬ: главная страница (сетка ячеек)
│   └── pudu-pos-screen.component.ts        ← СОЗДАТЬ: POS-экран (перенести логику из текущего монолита)
├── components/                         ← БЕЗ ИЗМЕНЕНИЙ
│   ├── pos-dialog.component.ts
│   └── dialogs/
│       └── ... (все 12 диалогов)
├── data/
│   ├── mock-data.ts                    ← БЕЗ ИЗМЕНЕНИЙ
│   ├── ne-error-codes.ts               ← БЕЗ ИЗМЕНЕНИЙ
│   └── catalog-entries.ts              ← СОЗДАТЬ: данные ячеек каталога
└── types.ts
```

### 4.2. Типы данных для каталога

```typescript
// Добавить в types.ts

export type CellCategory = 'context-order' | 'context-main' | 'scenario' | 'modal' | 'notification';

export interface CatalogCell {
  id: string;                     // Уникальный ID ячейки
  label: string;                  // Название (на русском)
  description: string;            // Описание (1-2 строки)
  icon: string;                   // Имя иконки Lucide
  iconColor: string;              // HEX-цвет иконки
  category: CellCategory;         // Категория (для группировки)
  modalType?: PuduModalType;      // Какую модалку открыть (для одиночных)
  scenario?: string;              // Какой сценарий запустить (для цепочек)
  context?: 'order' | 'main';    // Контекст POS-экрана (для контекстных ячеек)
  badge?: string;                 // Опциональный бейдж (напр. "BLOCKED", "AUTO")
  badgeColor?: string;            // Цвет бейджа
}

export interface CatalogSection {
  title: string;                  // Заголовок секции
  icon: string;                   // Иконка секции (Lucide)
  description: string;            // Подзаголовок-описание
  category: CellCategory;        // Фильтр по категории
  cells: CatalogCell[];          // Ячейки секции
}
```

### 4.3. Маршруты (обновлённые)

```typescript
// pudu-plugin.routes.ts

import { Routes } from '@angular/router';

export const PUDU_PLUGIN_ROUTES: Routes = [
  {
    path: '',
    loadComponent: () =>
      import('./pudu-plugin-prototype.component').then(
        m => m.PuduPluginPrototypeComponent
      ),
    children: [
      {
        path: '',
        loadComponent: () =>
          import('./screens/pudu-catalog-screen.component').then(
            m => m.PuduCatalogScreenComponent
          ),
      },
      {
        path: 'pos',
        loadComponent: () =>
          import('./screens/pudu-pos-screen.component').then(
            m => m.PuduPosScreenComponent
          ),
      },
    ],
  },
];
```

### 4.4. Корневой компонент (упрощённый)

```typescript
// pudu-plugin-prototype.component.ts — УПРОСТИТЬ до:

@Component({
  selector: 'app-pudu-plugin-prototype',
  standalone: true,
  imports: [RouterOutlet],
  template: `<router-outlet></router-outlet>`,
})
export class PuduPluginPrototypeComponent {}
```

### 4.5. Каталог-экран (новый главный)

**Файл**: `screens/pudu-catalog-screen.component.ts`

**Особенности стилизации** — страница каталога использует **светлую тему** (как остальные прототипы в рабочей области), а POS-диалоги продолжают использовать **тёмную POS-тему**:

```
Каталог (светлый фон)  →  клик на ячейку  →  POS-экран (тёмный фон) + диалог
```

**Layout каталога**:

```html
<div class="p-6 max-w-5xl mx-auto">

  <!-- Header -->
  <div class="mb-8">
    <h1 class="text-2xl font-semibold text-gray-900">
      PUDU — Каталог состояний плагина
    </h1>
    <p class="text-gray-500 mt-1">
      Нажмите на ячейку, чтобы открыть соответствующее состояние POS-терминала
    </p>
  </div>

  <!-- Секции -->
  <div *ngFor="let section of sections" class="mb-8">

    <!-- Заголовок секции -->
    <div class="flex items-center gap-2 mb-4">
      <lucide-icon [name]="section.icon" [size]="20" class="text-gray-400"></lucide-icon>
      <h2 class="text-lg font-medium text-gray-700">{{ section.title }}</h2>
      <span class="text-sm text-gray-400">{{ section.description }}</span>
    </div>

    <!-- Сетка ячеек -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      <div *ngFor="let cell of section.cells"
           (click)="onCellClick(cell)"
           class="relative bg-white rounded-xl border border-gray-200 p-5
                  hover:shadow-md hover:border-blue-300 cursor-pointer
                  transition-all duration-200 group">

        <!-- Бейдж (опциональный) -->
        <span *ngIf="cell.badge"
              class="absolute top-3 right-3 text-xs font-medium px-2 py-0.5 rounded-full"
              [style.background-color]="cell.badgeColor + '20'"
              [style.color]="cell.badgeColor">
          {{ cell.badge }}
        </span>

        <!-- Иконка в цветном круге -->
        <div class="w-12 h-12 rounded-xl flex items-center justify-center mb-3"
             [style.background-color]="cell.iconColor + '20'">
          <lucide-icon [name]="cell.icon" [size]="24"
                       [style.color]="cell.iconColor"></lucide-icon>
        </div>

        <!-- Заголовок -->
        <h3 class="font-medium text-gray-900 mb-1 group-hover:text-blue-600 transition-colors">
          {{ cell.label }}
        </h3>

        <!-- Описание -->
        <p class="text-sm text-gray-500 line-clamp-2">{{ cell.description }}</p>
      </div>
    </div>
  </div>
</div>
```

### 4.6. Логика навигации при клике на ячейку

```typescript
onCellClick(cell: CatalogCell): void {
  // Формируем query-параметры для POS-экрана
  const queryParams: any = {};

  if (cell.modalType) {
    queryParams.modal = cell.modalType;           // Открыть конкретную модалку
  }
  if (cell.scenario) {
    queryParams.scenario = cell.scenario;          // Запустить сценарий
  }
  if (cell.context) {
    queryParams.context = cell.context;            // Установить контекст POS
  }
  if (cell.category === 'notification') {
    queryParams.notification = cell.id;            // Показать уведомление
  }

  this.router.navigate(['pos'], {
    relativeTo: this.route,
    queryParams,
  });
}
```

### 4.7. POS-экран (перенос логики)

**Файл**: `screens/pudu-pos-screen.component.ts`

Этот компонент — **рефакторинг** текущего `pudu-plugin-prototype.component.ts`. Перенести сюда:

- Имитацию POS-экрана (header заказа, блюда, кнопки сценариев)
- Логику переключения `currentContext` (читать из `queryParams.context`)
- Логику `activeModal` (читать из `queryParams.modal`)
- Запуск сценариев (читать из `queryParams.scenario`)
- Toast-уведомления (читать из `queryParams.notification`)
- Все `*ngIf`-блоки для диалогов

**Инициализация из queryParams**:

```typescript
private route = inject(ActivatedRoute);

ngOnInit(): void {
  this.route.queryParams.subscribe(params => {
    // Установить контекст
    if (params['context']) {
      this.currentContext = params['context'] as 'order' | 'main';
    }

    // Открыть модалку
    if (params['modal']) {
      this.activeModal = params['modal'] as PuduModalType;
    }

    // Запустить сценарий
    if (params['scenario']) {
      this.runScenario(params['scenario']);
    }

    // Показать уведомление
    if (params['notification']) {
      this.showNotification(params['notification']);
    }
  });
}
```

**Кнопка «Назад в каталог»** — добавить в header POS-экрана:

```html
<button (click)="backToCatalog()"
        class="flex items-center gap-1 text-gray-400 hover:text-white text-sm">
  <lucide-icon name="arrow-left" [size]="16"></lucide-icon>
  Каталог
</button>
```

```typescript
backToCatalog(): void {
  this.router.navigate(['..'], { relativeTo: this.route });
}
```

---

## Раздел 5. Стилизация ячеек

### 5.1. Цветовая кодировка по секциям

| Секция | Цвет иконки заголовка | Фон ячейки | Цвет border при hover |
|--------|----------------------|------------|----------------------|
| Контекст: Из заказа | `text-blue-500` | `bg-white` | `border-blue-300` |
| Контекст: Главный экран | `text-purple-500` | `bg-white` | `border-purple-300` |
| Сценарии | `text-amber-500` | `bg-white` | `border-amber-300` |
| Состояния диалогов | `text-gray-500` | `bg-white` | `border-gray-300` |
| Уведомления | `text-red-500` | `bg-white` | `border-red-300` |

### 5.2. Бейджи

| Тип | Текст | Цвет фона | Цвет текста |
|-----|-------|-----------|-------------|
| Заблокировано | `BLOCKED` | `bg-red-50` | `text-red-600` |
| Автоматический | `AUTO` | `bg-blue-50` | `text-blue-600` |
| Новое (v1.1) | `v1.1` | `bg-green-50` | `text-green-600` |

---

## Раздел 6. Реализация сценариев (цепочки переходов)

Сценарии — это **автоматические последовательности** открытия модалок с задержками. При клике на ячейку-сценарий, POS-экран открывает первую модалку, и каждое следующее действие запускается автоматически.

```typescript
private scenarioChains: Record<string, ScenarioStep[]> = {
  'qr-full': [
    { modal: 'qr_cashier_phase', delay: 0 },
    { modal: 'loading', delay: 5000 },          // ускорено для демо (вместо 30с)
    { modal: 'qr_guest_phase', delay: 3000 },
    { modal: 'qr_success', delay: 5000 },       // ускорено для демо
  ],
  'qr-timeout': [
    { modal: 'qr_cashier_phase', delay: 0 },
    { modal: 'loading', delay: 5000 },
    { modal: 'qr_guest_phase', delay: 3000 },
    { modal: 'qr_timeout', delay: 5000 },
  ],
  'send-menu-ok': [
    { modal: 'send_menu_confirm', delay: 0 },
    { modal: 'loading', delay: 2000 },
    { modal: 'success', delay: 3000 },
  ],
  'send-menu-err': [
    { modal: 'send_menu_confirm', delay: 0 },
    { modal: 'loading', delay: 2000 },
    { modal: 'error', delay: 3000 },
  ],
  'cleanup-ok': [
    { modal: 'cleanup_confirm', delay: 0 },
    { modal: 'loading', delay: 2000 },
    { modal: 'success', delay: 3000 },
  ],
  'cleanup-multi-ok': [
    { modal: 'cleanup_multi_select', delay: 0 },
    { modal: 'loading', delay: 2000 },
    { modal: 'success', delay: 3000 },
  ],
};

runScenario(scenarioId: string): void {
  const chain = this.scenarioChains[scenarioId];
  if (!chain) return;

  let totalDelay = 0;
  chain.forEach((step, index) => {
    totalDelay += step.delay;
    setTimeout(() => {
      this.activeModal = step.modal as PuduModalType;
    }, totalDelay);
  });
}
```

> **Важно**: При ручном закрытии модалки пользователем — **прерывать** сценарий (очистить все setTimeout). Пользователь должен иметь возможность закрыть и вернуться в каталог.

---

## Раздел 7. Чеклист проверки

После реализации проверь:

- [ ] Главная страница показывает сетку из **26 ячеек** в **5 секциях**
- [ ] Клик на любую ячейку секции «Состояния» → открывает POS-экран с соответствующей модалкой
- [ ] Клик на ячейки секции «Контексты» → POS-экран в правильном контексте (order/main)
- [ ] Клик на ячейки секции «Сценарии» → автоматическая цепочка переходов
- [ ] Клик на ячейки секции «Уведомления» → POS-экран + toast
- [ ] Кнопка «Назад в каталог» работает на каждом POS-экране
- [ ] Все 12 диалогов (М1–М12) доступны и корректно отрисовываются
- [ ] E-STOP уведомление пульсирует каждые 5 сек
- [ ] Маркетинговый круиз — toggle работает
- [ ] Цепочки сценариев прерываются при ручном закрытии
- [ ] Нет ошибок компиляции (`ng build`)
- [ ] Маршрут зарегистрирован в `app.routes.ts` (уже есть, проверить что children не сломали)

---

## Раздел 8. Что НЕ менять

- **Компоненты диалогов** (`components/dialogs/*.ts`) — оставить как есть
- **POS-dialog обёртку** (`components/pos-dialog.component.ts`) — оставить как есть
- **Mock-данные** (`data/mock-data.ts`, `data/ne-error-codes.ts`) — оставить как есть
- **Стилизацию POS-темы** — тёмная тема для диалогов не меняется
- **Регистрацию в `app.routes.ts`** — путь `/prototype/front-pudu-plugin` уже зарегистрирован

---

## Приложение A. Полный массив данных каталога (справочный)

```typescript
// data/catalog-entries.ts

import { CatalogCell, CatalogSection } from '../types';

const CONTEXT_ORDER_CELLS: CatalogCell[] = [
  {
    id: 'ctx-order-send-menu',
    label: 'Отправить меню',
    description: 'Робот везёт физическое меню от кассы к столу гостя. Стол определён из заказа',
    icon: 'utensils',
    iconColor: '#b8c959',
    category: 'context-order',
    modalType: 'send_menu_confirm',
    context: 'order',
  },
  {
    id: 'ctx-order-cleanup',
    label: 'Уборка посуды',
    description: 'Робот едет к столу для сбора посуды. Один стол из контекста заказа',
    icon: 'spray-can',
    iconColor: '#b8c959',
    category: 'context-order',
    modalType: 'cleanup_confirm',
    context: 'order',
  },
  {
    id: 'ctx-order-send-dish',
    label: 'Доставка блюд [BLOCKED]',
    description: 'Функционал заблокирован — нет терминала на раздаче',
    icon: 'package',
    iconColor: '#ef4444',
    category: 'context-order',
    modalType: 'send_dish_blocked',
    context: 'order',
    badge: 'BLOCKED',
    badgeColor: '#ef4444',
  },
];

const CONTEXT_MAIN_CELLS: CatalogCell[] = [
  {
    id: 'ctx-main-cleanup-multi',
    label: 'Уборка посуды (мультивыбор)',
    description: 'Выбор нескольких столов одновременно с главного экрана',
    icon: 'spray-can',
    iconColor: '#b8c959',
    category: 'context-main',
    modalType: 'cleanup_multi_select',
    context: 'main',
  },
  {
    id: 'ctx-main-marketing',
    label: 'Маркетинговый круиз',
    description: 'Toggle запуска маркетингового патрулирования зала роботом',
    icon: 'megaphone',
    iconColor: '#3b82f6',
    category: 'context-main',
    context: 'main',
  },
];

const SCENARIO_CELLS: CatalogCell[] = [
  {
    id: 'scenario-qr-full',
    label: 'QR-оплата (полный цикл)',
    description: 'Кассир → робот к гостю → сканирование QR → оплата успешна',
    icon: 'qr-code',
    iconColor: '#b8c959',
    category: 'scenario',
    scenario: 'qr-full',
    context: 'order',
  },
  {
    id: 'scenario-qr-timeout',
    label: 'QR-оплата (тайм-аут)',
    description: 'Гость не успел оплатить → робот возвращается на базу',
    icon: 'clock',
    iconColor: '#f97316',
    category: 'scenario',
    scenario: 'qr-timeout',
    context: 'order',
  },
  {
    id: 'scenario-send-menu-ok',
    label: 'Отправка меню → Успех',
    description: 'Подтверждение → загрузка → задача создана успешно',
    icon: 'check-circle-2',
    iconColor: '#22c55e',
    category: 'scenario',
    scenario: 'send-menu-ok',
    context: 'order',
  },
  {
    id: 'scenario-send-menu-err',
    label: 'Отправка меню → Ошибка',
    description: 'Подтверждение → загрузка → ошибка (робот недоступен)',
    icon: 'alert-circle',
    iconColor: '#ef4444',
    category: 'scenario',
    scenario: 'send-menu-err',
    context: 'order',
  },
  {
    id: 'scenario-cleanup-ok',
    label: 'Уборка → Успех',
    description: 'Подтверждение уборки → загрузка → задача создана',
    icon: 'check-circle-2',
    iconColor: '#22c55e',
    category: 'scenario',
    scenario: 'cleanup-ok',
    context: 'order',
  },
  {
    id: 'scenario-cleanup-multi-ok',
    label: 'Уборка (мульти) → Успех',
    description: 'Мультивыбор столов → загрузка → задача создана',
    icon: 'check-circle-2',
    iconColor: '#22c55e',
    category: 'scenario',
    scenario: 'cleanup-multi-ok',
    context: 'main',
  },
  {
    id: 'scenario-unmapped',
    label: 'Стол не замаплен',
    description: 'Попытка действия со столом без привязки к точке робота',
    icon: 'map-pin-off',
    iconColor: '#f97316',
    category: 'scenario',
    scenario: 'unmapped',
    context: 'order',
  },
];

const MODAL_CELLS: CatalogCell[] = [
  {
    id: 'modal-send-menu',
    label: 'М1: Подтверждение отправки меню',
    description: 'Карточка задачи: стол, робот, фразы при заборе и у стола',
    icon: 'utensils',
    iconColor: '#b8c959',
    category: 'modal',
    modalType: 'send_menu_confirm',
  },
  {
    id: 'modal-cleanup',
    label: 'М2: Подтверждение уборки',
    description: 'Карточка задачи: стол, робот, фраза, время ожидания',
    icon: 'spray-can',
    iconColor: '#b8c959',
    category: 'modal',
    modalType: 'cleanup_confirm',
  },
  {
    id: 'modal-qr-cashier',
    label: 'М3: QR — фаза «Кассир»',
    description: 'Иконка принтера, фраза робота, таймер 30 сек',
    icon: 'printer',
    iconColor: '#b8c959',
    category: 'modal',
    modalType: 'qr_cashier_phase',
  },
  {
    id: 'modal-qr-guest',
    label: 'М4: QR — фаза «Гость»',
    description: 'Mock QR-код, таймер 120 сек, кнопка подтверждения',
    icon: 'qr-code',
    iconColor: '#b8c959',
    category: 'modal',
    modalType: 'qr_guest_phase',
  },
  {
    id: 'modal-qr-success',
    label: 'М5: QR — Оплата прошла',
    description: 'Иконка успеха, автозакрытие через 3 секунды',
    icon: 'check-circle-2',
    iconColor: '#22c55e',
    category: 'modal',
    modalType: 'qr_success',
  },
  {
    id: 'modal-qr-timeout',
    label: 'М6: QR — Тайм-аут',
    description: 'Гость не оплатил, робот возвращается на базу',
    icon: 'clock',
    iconColor: '#f97316',
    category: 'modal',
    modalType: 'qr_timeout',
  },
  {
    id: 'modal-unmapped',
    label: 'М7: Стол не замаплен',
    description: 'Предупреждение + ссылка на iikoWeb для настройки маппинга',
    icon: 'map-pin-off',
    iconColor: '#f97316',
    category: 'modal',
    modalType: 'unmapped_table',
  },
  {
    id: 'modal-dish-blocked',
    label: 'М8: Доставка блюд [BLOCKED]',
    description: 'Заглушка: баннер «Требуется решение», пометка «В разработке»',
    icon: 'package',
    iconColor: '#ef4444',
    category: 'modal',
    modalType: 'send_dish_blocked',
    badge: 'BLOCKED',
    badgeColor: '#ef4444',
  },
  {
    id: 'modal-loading',
    label: 'М9: Loading',
    description: 'Универсальный спиннер загрузки. Светлая тема, автозакрытие 3 сек',
    icon: 'loader-2',
    iconColor: '#6b7280',
    category: 'modal',
    modalType: 'loading',
  },
  {
    id: 'modal-success',
    label: 'М10: Задача создана',
    description: 'Универсальный диалог успеха. Автозакрытие 2 сек',
    icon: 'check-circle-2',
    iconColor: '#22c55e',
    category: 'modal',
    modalType: 'success',
  },
  {
    id: 'modal-error',
    label: 'М11: Ошибка',
    description: 'Универсальный диалог ошибки. Светлая тема, кнопки Повторить/Закрыть',
    icon: 'alert-circle',
    iconColor: '#ef4444',
    category: 'modal',
    modalType: 'error',
  },
  {
    id: 'modal-cleanup-multi',
    label: 'М12: Мультивыбор столов',
    description: 'Сетка столов 3 колонки, toggle-выбор, disabled для незамапленных',
    icon: 'layout-grid',
    iconColor: '#b8c959',
    category: 'modal',
    modalType: 'cleanup_multi_select',
  },
];

const NOTIFICATION_CELLS: CatalogCell[] = [
  {
    id: 'notify-estop',
    label: 'E-STOP (аварийная остановка)',
    description: 'Toast-уведомление об аварийной кнопке робота. Повторяется каждые 5 сек',
    icon: 'octagon',
    iconColor: '#ef4444',
    category: 'notification',
  },
  {
    id: 'notify-ne-error',
    label: 'Ошибка связи NE',
    description: 'Toast-уведомление о потере связи с API Next Era',
    icon: 'wifi-off',
    iconColor: '#f97316',
    category: 'notification',
  },
];

export const CATALOG_SECTIONS: CatalogSection[] = [
  {
    title: 'Контекст: Из заказа',
    icon: 'receipt',
    description: 'Действия, вызванные из экрана заказа iikoFront',
    category: 'context-order',
    cells: CONTEXT_ORDER_CELLS,
  },
  {
    title: 'Контекст: Главный экран',
    icon: 'monitor',
    description: 'Действия с главного экрана iikoFront (нет контекста стола)',
    category: 'context-main',
    cells: CONTEXT_MAIN_CELLS,
  },
  {
    title: 'Сценарии (цепочки переходов)',
    icon: 'workflow',
    description: 'Автоматические демонстрации полных user journey',
    category: 'scenario',
    cells: SCENARIO_CELLS,
  },
  {
    title: 'Состояния диалогов',
    icon: 'layout-grid',
    description: 'Каждая ячейка — одно конкретное модальное окно',
    category: 'modal',
    cells: MODAL_CELLS,
  },
  {
    title: 'Уведомления и специальные состояния',
    icon: 'bell',
    description: 'Toast-уведомления и overlay-состояния',
    category: 'notification',
    cells: NOTIFICATION_CELLS,
  },
];
```

---

## Приложение B. Диаграмма навигации

```
/prototype/front-pudu-plugin
│
├── / (PuduCatalogScreenComponent — сетка ячеек, светлая тема)
│   │
│   ├── [клик: ячейка контекста] ──► /pos?context=order&modal=send_menu_confirm
│   ├── [клик: ячейка сценария]  ──► /pos?context=order&scenario=qr-full
│   ├── [клик: ячейка модалки]   ──► /pos?modal=cleanup_confirm
│   └── [клик: ячейка уведомл.]  ──► /pos?notification=notify-estop
│
└── /pos (PuduPosScreenComponent — POS-терминал, тёмная тема)
    │
    ├── queryParams.context → устанавливает order/main экран
    ├── queryParams.modal → открывает конкретную модалку
    ├── queryParams.scenario → запускает цепочку
    ├── queryParams.notification → показывает toast
    │
    └── [кнопка «← Каталог»] ──► /prototype/front-pudu-plugin
```
